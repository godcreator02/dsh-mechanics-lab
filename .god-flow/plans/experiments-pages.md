# plan：把剩余说明页做完

## 现状

58 项里 17 项有页面，但**分两种规格**：

| | 项数 | 规格 |
|---|---|---|
| `00-base` ＋ `01-entry` | 11 | 现行规格：接 `lab.css` / `lab.js`、有原理层、侧边文件树、`.picker` 枚举组件 |
| `02` / `05`–`09` 各一页 | 6 | 旧规格：页内自带全部样式与脚本，没有原理层、没有文件树、没有 picker |

骨架已经收敛完：`experiments/lab.css`（8KB）与 `experiments/lab.js`（3.5KB）装着
版心、侧栏两窗、ASCII 文件树、`.picker`、`.sbar`、调色板三层、标题、按钮、判定卡。
**新页只写本页内容**，页内 `<style>` 实测 1.7–3.0KB。

写法正本是 `docs/page-writing.md`，派 agent 时让它整篇照做，本计划不复述。

## 剩余工作量：47 个页面任务

**翻新 6 页**（旧规格 → 现行规格）：`02-recipe/layer-stack`、`05-reload/watch-root`、
`06-inject/activation-order`、`07-tree/recipe-vs-tree`、`08-service-core/one-owner`、
`09-boot-vs-runtime/duplicate-id-timing`。

**新建 41 页**，其中 **27 项有用例**、**14 项 0 用例**（走未覆盖页形态：整页标未覆盖、
画缺口、三层视觉信号，成本比正常页低得多）。

## 分批

一批 5–6 个 agent 并行，验完再下一批。

| 批 | 内容 | 项数 |
|---|---|---|
| 一 | 翻新那 6 页旧规格 | 6 |
| 二 | `02-recipe` 3 项 ＋ `04-replay` 2 项 | 5 |
| 三 | `03-supply` 5 项 | 5 |
| 四 | `05-reload` 5 项 | 5 |
| 五 | `06-inject` 2 ＋ `07-tree` 2 ＋ `09-boot-vs-runtime` 2 | 6 |
| 六 | `08-service-core` 6 项 | 6 |
| 七 | 0 用例的未覆盖页，前 7 项 | 7 |
| 八 | 0 用例的未覆盖页，后 7 项 | 7 |

⚠️ 七、八两批可以多派几个并行——未覆盖页没有归档数据要核，形态又是固定的。

## 骨架不下放

**共享的 CSS 与 JS 由主会话写，agent 只写本页内容。** 这条是买来的：上一轮五个 agent
并行写同一个组件，长出了五种实现（`cnode` / `mstep` / `step-btn` / `mech-item` /
`steplist`）和三份 CSS 变体。

要动 `lab.css` / `lab.js` 时，主会话改完再派活；agent 的任务书里写死「不许改这两个文件、
不许另起类名」。

## 验收：每批四步，一步都不能省

1. **两个脚本全绿**——`scratchpad/gate.py`（外链、双主题、`test_xxx` 真实存在、
   无自动播放、归档目录存在、只引 `lab.css` / `lab.js`）与 `scratchpad/verify_inline.py`
   （页面内联的文件原文跟磁盘逐字节比对）
2. **浏览器里把每个 picker 项、每个可点文件、每个折叠开关都点一遍**，收 `onerror`。
   门禁抓不到运行时报错——`config-delivery` 的 `initPicker` 撞 null 就是这么漏出去的，
   而那个 agent 的自查报告是全绿的
3. **截图看视觉**——列宽、折行、对齐。`field-vocabulary` 的表格第三列被挤到 48px、
   「必填与否」竖成一字一行，脚本一个字都查不出来
4. **读 agent 改了什么，别只扫结论**——`isolation-guarantees` 那次把 `bmHighlight()`
   留下改指向新侧栏，报告里写的是「统一改指向新侧栏」，听着像在遵守规格，实际是反的

做完更新 `experiments/index.html` 的 `HAS_PAGE` 集合，然后提交。

## 特殊项

- **`03-supply/install-command`**：1 条用例但**没有归档**。数字无处可取，页面上
  一个数字都不许有，按「有机制依据、无观测数据」的形态做，覆盖程度如实标
- **`09/boot-failure-shapes`**：9 个 profile、8 条用例，本组最重的一项
- **`02-recipe/override-semantics`**：6 个 profile、6 条用例，计划里标「很值得画」
  （覆盖前后的键差——整键赋值不是深合并）
- **`08-service-core`**：10 项里 3 项 0 用例，组内混合，分批时别把它们混进有用例那批

## 已知的坑

按踩到的顺序记，都是会重复发生的：

- **内联文件原文不能走「读出来再抄进去」**。工具展示文本会把 CRLF 显示成 LF、
  把 NUL 这类不可见字节吞掉，照抄就跟磁盘对不上而且看不出来。必须字节级读写
- **门禁扫「页面行为」的几项要先摘掉 `<pre>`**。内联的真实源码里有 `setInterval`，
  被当成自动播放。否则页面越忠实于原文越容易被判违规，逼人改内容迎合检查
- **PowerShell 的数组会扁平化**。`@(@(a,b))` 摊成两个字符串后 `$pair[0]` 取到的是
  首字符，`.Replace('v','a')` 把全文的 `v` 换成了 `a`。批量替换用 Python 写显式映射表
- **自己的正则也会骗人**。`^[^\{\}]*` 里 `[^\{\}]` 包含换行，能从文件开头一路吞到
  几百行之后，拼出来的「内容错乱」是工具造的假象。核内容用 DOM 或字节比对，别用正则
- **改 CSS 层叠顺序要有安全网**。抽共享样式会改变规则先后，用全元素计算样式快照
  前后对比兜底。**统一飘掉的规则时不能要求 0 差异**，要逐条审差异是不是预期内的
- **Chrome 截图会 CDP 超时**，重试一次通常好；连续失败改用脚本量（`getBoundingClientRect`
  比截图更准，也不受窗口宽度影响）
- **改了文件要加查询参数才刷新**（`?v=2`），否则读缓存
- **同一个标签页里上一轮的点击状态会留下**，误判成 bug。核默认状态要干净加载后立刻读

## 待拍的决策

下个会话开工前问用户：

1. **14 项 0 用例的未覆盖页要不要先做**。它们成本最低、能最快把覆盖地图填满；
   但它们不产出新知识，先做有用例的那 27 项对读者价值更高
2. **翻新那 6 页要不要顺带补原理层**。它们现在只有观测和判定两层，补齐四层等于重写，
   工作量接近新建；只换骨架不补原理层则组内规格仍不一致
