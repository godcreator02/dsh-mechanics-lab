# 探索历史

这个目录装**探索史**：做过的实验、放弃的路线、推翻的判断、方法上的转向。
每篇记一次**了断**——一件改变了下一步动作的事。

## 跟现状文档的分工

    现状文档   本目录  
 --- --- --- 
  装什么   项目此刻是什么样   探索史：实验、放弃的路线、决策  
  形态   **覆盖式更新**，过期就改   **一次了断后冻结**，写完不改  
  判据   这句话半年后还代表当前真相吗   这承载的是探索意志吗  
  在哪   `CLAUDE.md` / `GLOSSARY.md` / `SYLLABUS.md` / 各课 README   这里  

两条界线：

- **当下成立的理由属于现状，不属于历史。**「`--dump-config` 会重写 `cordis.yml`，
  所以取证要在 dump 之前」是现状，写在脚手架注释里天经地义；「这条是补读文档时
  发现的、之前一直当只读操作用」是历史，进这里
- **一件事在两处各有一句话不算两份真相。**「兜底 hmr 与 include 平级」是现状，
  「这条怎么发现的、推翻了什么」是历史，两个不同的事实

现状文档**不装**历史叙事（推翻、演进、事故史）、时间戳、changelog；
本目录**不装**现状与待办——篇是冻结的，装现状等于生产假消息。

## 写法

每篇两个必填块：

- **`## 背景`** —— 不知情的读者需要先知道什么。判据：把这一篇单独发给没参与过
  这个项目的人，他能不能读懂后面在讲什么
- **`## 结论与边界`** —— 一句话结论，**紧跟适用边界**：什么条件下验过、什么条件外
  **没验过**。边界必须与结论同段，不许另起一节——只读首段的人会把结论当无条件
  事实用，那正是幻觉的典型形态

可选块：`## 经过`（**复现路径**，不是心路历程——判据是「读者能不能照着重跑一遍」）、
`## 依据`（从观测到结论的那一步推理及其前提）、`## 否掉的候选`（每条注明死在哪个
观测上）、`## 代价与遗留`。**用不上的块直接不写，绝不留空标题**——空标题伪装成
有结构，下一个读到的人会以为这里本该有内容。

举证：正文出现的关键数字，要么挂**受控路径**（仓内进版本控制的路径、commit 号），
要么**显式写「无物证」**。不许指 `.testhome/`（每次跑都清）或 `results/` 里未提交的
部分。「无物证」不是丢人的标记，是诚实的标记——比编一个路径强，也比让读者以为
有证据强。

禁：TL;DR 摘要段（重点靠结论块前置自然浮出）、心路历程（转写进「否掉的候选」并
注明死在哪）、事实与推理混排在同一段（分不清哪句可核验）。

## 命名与顺序

`NNNN-<短名>.md`，四位序号即落篇顺序。不用日期做文件名——同一天常有多次了断，
序号比日期稳。每篇篇头标了断日期。

## 篇目

  篇   了断   日期  
 --- --- --- 
  [0001-实验台从下游视角转向独立研究](0001-turn-to-independent-research.md)   转向   2026-08-15  
  [0002-E3 观测信号选错导致整批数据作废](0002-e3-wrong-observation-signal.md)   走死   2026-08-15  
  [0003-PENDING 致命性悬案与用例的假绿](0003-pending-fatality-and-false-green.md)   推翻   2026-08-16  
  [0004-裸包名锚点的机制解释推翻](0004-bare-specifier-anchor-refuted.md)   推翻   2026-08-16  
  [0005-补读官方文档：12 条重复劳动](0005-official-docs-catch-up.md)   转向   2026-08-16  
  [0006-教学目录按加载链条重排](0006-syllabus-reordered.md)   转向   2026-08-16  
  [0007-六课并行交给 subagent](0007-six-lessons-in-parallel.md)   采纳   2026-08-16  
