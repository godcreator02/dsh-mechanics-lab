# E3 — hmr 是否拥有 patch 监听的生死（含 dshw attach 走钢丝风险）

跑于 2026-08-15 20:43:23（本地时间）
dsh 部署：C:\Users\godamericatb14\AppData\Local\npm-cache\_npx\1e7f6d9597241db0\node_modules\@deepseek-ai\dsh\lib\bin.js

## 判定

假说 A：不成立或部分不成立：至少有一次改活层没有… / 假说 B：成立，且精确命中预测的转折点：插 hmr… / 假说 C：坏消息（重大发现）：只改 hmr 条目的…

## 细节

### Part A（lab-a：boot 即带 hmr 常驻条）

| 轮次 | 生效 | appliedAt 变化 | config.tag | 备注 |
|---|---|---|---|---|
| A0 起手态（未 insert） | True |  |  | 实例仍在线（根路径有响应），路由确实没挂/没换过来 |
| A1 insert 探针（第1次改） | True |  |  |  |
| A2 config→round2（第2次改） | False | True |  | 读到的 tag=，实例仍在线（根路径有响应），路由确实没挂/没换过来 |
| A3 disable 探针（第3次改）→应 404 | True |  |  |  |

### Part C（复用 lab-a：只改 hmr 条目的 config，模拟 dshw attach）

| 轮次 | 生效 | appliedAt 变化 | config.tag | 备注 |
|---|---|---|---|---|
| C1 只改 hmr config（落定检查，探针应仍是 round2 不变） | True |  | round2 | 这轮本身不是假说 C 的判定信号，只是确认改 hmr 没有当场炸掉实例 |
| C2 改 hmr.config 后再改探针 config（假说 C 核心判定） | False | True | round2 | 读到的 tag=round2，appliedAt=08/15/2026 12:42:09，实例仍在线（根路径有响应），路由确实没挂/没换过来 |
| C2b 二次确认（多等 8 秒） | False |  | round2 | 若仍未生效，判定为真死而非慢 |
| C3 hmr root 改回 []（落定检查） | False |  | round2 |  |
| C4 hmr revert 之后探针 config 是否还能再动 | False |  | round2 | 哑火是永久性的，改回 root 也救不回来（符合"游离 hmr 已被 dispose，patch 监听不会再回来"的预期） |

### Part B（lab-b：boot 时活层无 hmr 条，出厂形态）

| 轮次 | 生效 | appliedAt 变化 | config.tag | 备注 |
|---|---|---|---|---|
| B0 起手态（未 insert） | True |  |  |  |
| B1 insert 探针（第1次改） | True |  |  |  |
| B2 config→round2（第2次改） | True | True | round2 |  |
| B3 追加 hmr 条目（落定检查，探针此轮本不该变） | True |  | round2 | 这轮本身不是判定信号，只确认探针没被这次改动带崩 |
| B4 hmr 条目落地后再改探针 config（假说 B 核心判定） | False |  | round2 | 读到的 tag=round2（预期仍卡在 round2），实例仍在线（根路径有响应），路由确实没挂/没换过来 |
| B4b 二次确认（多等 8 秒） | False |  | round2 | 若仍未生效，判定为真死（活层从此哑火直到重启），不是慢 |
| B5 再改一次确认彻底哑火 | False |  | round2 |  |

### 假说 A（场景对照：boot 时活层就有 hmr 常驻条）

不成立或部分不成立：至少有一次改活层没有生效，细节见下表——与"树拥有的 hmr 长期稳定"的预期不符，需要进一步排查。

### 假说 B（场景对照：boot 时活层没有 hmr 条目，出厂形态）

成立，且精确命中预测的转折点：插 hmr 条目之前两次改活层（insert / 改 config）都生效，插入 `- id: hmr / disabled: false` 之后活层重放彻底哑火（实例本身仍存活，只是没人再听 patch 文件变化）——"先活、后死"，转折点正是插入 hmr 常驻条那一刻。

### 假说 C（最要紧：改 hmr 条目的 config 是 dispose+重建还是原地 reconfigure）

坏消息（重大发现）：只改 hmr 条目的 config 之后，活层重放就彻底哑火了（即便把 root 改回原值也救不回来，实例本身仍存活）——说明改 config 触发了该条目 fiber 的 dispose+重建，hmr 自己注册的 patch 文件监听被自己的清理逻辑带走，而 watchUserPatches 不会再被重新调用。这意味着 `dshw attach` 每挂一次新插件，都会让当前活层从此失聪，直到主实例重启。这是 attach 机制的地基性缺陷，需要立刻处理（比如 attach 后强制走一次"压下-抬起"或提示用户重启）。

### lab-b 尾部日志（B3/B4 转折前后，供交叉核对）

```
--- lab-b.err.log ---
--- lab-b.out.log ---
dsh web: http://127.0.0.1:3091
```

