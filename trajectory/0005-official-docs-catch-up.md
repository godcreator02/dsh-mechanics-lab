# 补读官方文档：12 条重复劳动

> 了断于 2026-08-16 ｜ 转向 ｜ 冻结

## 背景

实验台前四课（L0–L2、L9）的做法是「读源码提出假说 → 实测验证」，官方文档站只在
最初扫过几眼。做完四课后被指出「文档你都没看全，文档里写得挺详细」。

## 结论与边界

**信息源要按「便宜且权威」排序取用：官方文档 → 源码 → 实测，三档都要走。**
跳过第一档的代价是 **12 条结论属于重新发现官方早已写明的东西**，其中两条还被
误记成「推翻项目正本的发现」——实际是项目正本抄错了文档。

边界：**不能倒过来只信文档**。同一次盘点里另有 12 条是文档完全没写、只能靠实测的，
它们全落在「框架怎么加载插件」这一层，而官方 `user/develop/` 讲的是「怎么写插件」。
另有 2 处文档在教程语境里做了简化，照抄会踩坑。三档互相校验才是对的做法。

盘点对照的是 commit `47f9438` 那一版文档，**没有**核对更早或更新的版本。

## 经过

把官方仓库 `docs/` 整个拉进 `docs/official/`：215 篇 markdown、2.93 MB，版本钉死在
commit `47f9438`（钉 commit 不跟 `master`——结论是对着某一版文档说「写了/没写/
不一样」的，拉新版会让所有对照失去基准）。中英双语各一棵树。

逐篇对照后分三类：

**A 类 · 文档已写明、我们绕远路重新发现（12 条）**

| 结论 | 文档出处 |
|---|---|
| FiberState 六态 + 完整状态图 | `zh/user/develop/framework/index.md` 有现成表格 |
| `config` 整字段替换、不深度合并 | `zh/user/develop/basic/publish.md:123` 原话 |
| 五层叠加顺序 | 同上 `:112-119`，逐条列明 |
| 裸包名从 dsh 安装目录解析 | 同上 `:128` |
| 相对路径以 profile 目录为锚 | `zh/user/develop/basic/index.md:56` |
| 改 `config` = 卸载重载 | `zh/user/develop/basic/config.md:100` |
| 服务消失自动 dispose、恢复自动重载 | `zh/user/develop/framework/index.md:38` |
| effect 逆序清理、异步并发不保证顺序 | 同上 `:63` |
| waterfall 必须 `next()` | `zh/user/develop/framework/events.md:80` |
| 状态转换发 `internal/status` | `zh/cordis-api/fiber.md` |
| `dsh plugin add` 自动追加 bundles | `zh/user/develop/basic/publish.md:83` |
| `!!js` 先等注入再求值 | 同上 `:151` |

**B 类 · 文档没写、实验才有价值（12 条）**：幽灵条目、boot 末尾审计、兜底判服务
不判条目、树的层级、`ERR_UNSUPPORTED_DIR_IMPORT`、包级 `services.required`、
条目级 `inject` 是补充、野字段静默带走、`cordis.yml` 被重写、兜底 hmr 的
`root: []` 不监听代码文件、bundle 层与活层是两条独立注册路径、同 id 双挂载的后果。

**C 类 · 需澄清（2 处）**：「插件路径必须是绝对路径」是教程语境下的建议而非解析
规则全部；「服务没就绪插件会等着」只描述运行期，boot 期永远等不到会杀进程。

物证：文档快照与盘点表在 commit `a5462af`，盘点全表在 `DRAFT.md` 第八节（该文件
后续被拆解，内容迁入本目录与现状文档）。

## 依据

A 类那 12 条集中在「怎么写插件」这一层——正是 `user/develop/` 四篇教程的覆盖范围。
B 类那 12 条集中在「框架怎么加载插件」——官方文档没有这一层的篇目。

这个分布不是偶然：**官方文档的受众是插件作者，不是研究加载机制的人**。所以
「文档没覆盖机制层」是可预期的，不构成「可以不读文档」的理由——恰恰相反，正因为
边界这么清晰，先读一遍就能把 12 条省下来。

## 否掉的候选

- **「文档写的是设计意图、未必是实际行为，所以以实测为准即可」** —— 这个说法本身
  成立（C 类那两处就是文档与实际有偏差），但它推不出「可以不先读」。文档说了什么
  正是**判断实测该测什么**的输入：知道文档承诺 X，用例才写得成「验证是不是 X」的
  对照式；不知道，就只能写成「跑跑看会怎样」的探索式，断言强度完全不同

## 代价与遗留

12 条重复劳动已经发生，无法回收。副产物是课程性质的重新分类：每一课现在标
📗 复述型 / 🔬 发现型 / ⚠️ 矫正型，**这个标记决定用例怎么写**——对照式还是探索式，
断言强度和失败时的含义完全不同。

遗留：官方仓库还有几个目录没读完——`cordis-api/` 五篇只读了 `fiber.md`，
`subsystems/` 92 篇几乎没动，`postmortem/` 只读了 0002。`fiber.md` 里白捡到
`fiber.getEffects()` 这个现成观测接口，说明剩下那些篇里大概率还有同类的东西。
未实施。
