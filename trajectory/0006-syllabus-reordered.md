# 教学目录按加载链条重排

> 了断于 2026-08-16 ｜ 转向 ｜ 冻结

## 背景

课程大纲原本是四级梯度：一个插件 → 插件之间 → 配方从哪来 → 什么是热的，
外加后补的「第零级 · 环境」（L0）。做完前四课并补读官方文档之后，三处不合用的
地方同时暴露出来。

## 结论与边界

**大纲改为顺着加载链条编排的六部分十七课**：你写的东西 → 声明它的那一行 →
那一行从哪来 → 变成什么 → 怎么活过来 → 活着的怎么变。L0 不拆，重新定位成
**全景课**（先总后分的「总」），后面六部分各展开它的一块。

同时给每课加**性质标记**（📗 复述型 / 🔬 发现型 / ⚠️ 矫正型），**它决定用例怎么写**。

边界：重排只动编排与定位，**没有动任何测试代码**——已完成课的用例一行未改，
重排后跑全套 70 个用例全过。已完成的四课（L0/L1/L2/L9）的实测结论也未受影响。

## 经过

三条理由推动重排：

**一、12 条结论从「发现」降级为「验证」。** 补读官方文档后发现它们文档已写明
（详见 0005 篇）。这不只是措辞问题——探索式用例写成「跑跑看会怎样」，对照式
用例写成「文档承诺 X，验证这一版部署是不是 X」，断言强度和失败时的含义完全不同。
配方那三课全部受影响。

**二、L0 挂在「第零级 · 环境」名不副实。** 它实际讲的是加载模型的核心——配方 ≠ 树、
`include` 是树根、层级决定谁会被重放动到——是理解后面所有课的前提，不是环境准备。

**三、原梯度有个真实的断裂。** 「插件之间」（服务、`inject`、激活）排在「配方从哪来」
前面，但逻辑上**先有树才谈得上激活**。做 L9 时就已经别扭：它要用 `disabled` 控制
就位时机，而那属于前一课；激活审计又要等 L0。

编号映射：原 L3（服务与 `inject`）→ L9（目录 `git mv`，git 识别为纯重命名）；
原配方三课 → L4/L5/L6；原 L4 → L10；原 L5 → L13；原 L6 → L12；原热四课 → L14–L17。
新增 L3、L7、L8、L11 四课，均从 L0 展开。

物证：重排后的大纲与全套 70 个用例通过的记录在 commit `b123add` 之后的提交序列；
L8 的识别过程见下方「否掉的候选」。

## 依据

新顺序的依据是**加载链条本身**——框架处理一个插件的实际顺序就是：读配方 → 组成树 →
按服务可用性激活 → 运行中变化。教学顺序跟着它走，每一步的前提都是上一步的产物，
不会再出现「先讲条目怎么活过来、再讲条目从哪来」这种断裂。

性质标记的依据是 0005 篇那次三类盘点：A 类（文档已写明）对应 📗，B 类（文档没写）
对应 🔬，C 类（文档简化）对应 ⚠️。

## 否掉的候选

- **保留四级结构、只加性质标记** —— 改动最小，但第三条断裂（激活排在配方前面）保留
  不动。断裂是结构性的，加标记治不了
- **把 L0 拆开、各块并入对应部分** —— 一度被采纳过，后来否掉：拆开之后没有任何一处
  能让读者先看到整棵树的形状。改成保留 L0 作全景课，是「先总后分」的总

顺带识别出一门原本不在大纲里的课：**L8「层级」**。它是拆 L0 时浮出来的，
而 L17（hmr 归属，全课程收口）的实验设计完全依赖它——不先分清测的是「兜底的 hmr」
还是「配方里的 hmr」，那一课注定重蹈 v1 的覆辙。

## 代价与遗留

课程编号整体变动，此前所有引用旧编号的地方都要回改（三份顶层文档共约 20 处）。
测试代码零改动，但一个课程目录改了名。

遗留：大纲里 L7–L8 夹在 L6 和 L9 之间，而它俩比 L9 更「深」——一个只想写插件的人
需要 `inject`（L9），未必需要知道幽灵条目和层级。依赖顺序与难度顺序在这里分叉。
处置是标了一条「写插件最短路径」（L7–L8 首次可跳过），**没有改顺序**——因为
「先有树才谈得上激活」这个依赖是真的。
