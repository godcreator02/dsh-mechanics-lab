# 实验台从 dshw 视角转向独立研究

> 了断于 2026-08-15 ｜ 转向 ｜ 冻结

## 背景

这个实验台最初是为搞清楚 dshw（DSH 工作区环境管理器）的几处疑问而搭的：
patch 层怎么叠、hmr 归谁管、改配置要不要重启。第一版做了六个实验（E1–E6），
全部以 dshw 的用法为参照系组织。

## 结论与边界

**研究对象改为 DSH 框架本身，dshw 降为它下游的一个消费者。** 实验选题从「原理链条」
出发而非「dshw 遇到的疑问」；教学插件为讲清原理而设计，不仿制真实插件；
结论产出后由 dshw 按结论校准自己，反过来不成立。

边界：**这是选题原则的转向，不是对 v1 结论的否定**。E1/E2/E4/E5/E6 的实测数据仍然
可信（用例之间互相印证过），迁入新课程时只是去掉 dshw 视角重新表述并用 Python
复跑对齐。唯一整个作废的是 E3，理由另见 0002 篇。

## 经过

v1 的症状有三个层次：

**选题层**：只覆盖「dshw 用到的那几个点」，不成链条。整个「插件是什么、插件之间
怎么依赖」一片空白，直接从第三级（配方合成）开始——因为 dshw 恰好不需要问前两级。

**表述层**：实验说明里写着「复刻生产现场的风险用例」「验证 dshw attach 走钢丝」。

**方法层**：最能说明问题的一句是「实验台要保真就得复刻 dshw 的绕行」——**主从颠倒**。
实验台去复刻消费者的绕行，那测出来的是绕行本身对不对，不是被绕的机制是什么。

物证：v1 的 PowerShell 版基线在 commit `c08eb8d`，六个实验的代码与结论均在其中。

## 依据

一个额外的观察坐实了这条转向：**最 dshw 视角的那个实验（E3），结果恰好是唯一
不可用的那个。**

因果不是巧合。E3 问的是「dshw 的某个用法安不安全」而不是「这个机制怎么工作」，
于是观测方法也跟着从 dshw 的关注点出发——盯着 dshw 会用到的那个探针，
而不是盯着被改动的那个条目。信号选错，整批数据作废（详见 0002 篇）。

**问题问错，观测就会跟着错**，这是选题原则能影响数据质量的具体路径。

## 代价与遗留

v1 的六个实验要用 Python 重写并复跑对齐。这个代价被接受，因为复跑同时提供了
另一层价值：**Python 版是独立的第二实现**，结论一致则是双实现交叉印证，
不一致则说明其中一版有 bug——两种情况都比只跑一遍强。

遗留：dshw 这个下游要按新结论校准的清单单独维护，不混进教材正文。
