# 实验台从下游视角转向独立研究

> 了断于 2026-08-15 ｜ 转向 ｜ 冻结

## 背景

这个实验台最初是为搞清楚**某个下游工具**的几处疑问而搭的：patch 层怎么叠、
hmr 归谁管、改配置要不要重启。第一版做了六个实验（E1–E6），全部以那个工具的
用法为参照系组织。

## 结论与边界

**研究对象改为 DSH 框架本身，实验台与任何下游工具脱钩。** 选题从「原理链条」出发
而非「某个工具遇到的疑问」；教学插件为讲清原理而设计，不仿制真实插件；
教材只讲框架原理，不出现具体工具的名字。

边界：**这是选题原则的转向，不是对已有实测数据的否定**。E1/E2/E4/E5/E6 的数据仍然
可信（用例之间互相印证过），迁入新课程时只是去掉下游视角重新表述并用 Python
复跑对齐。唯一整个作废的是 E3，理由另见 0002 篇。

## 经过

症状有三个层次：

**选题层**：只覆盖「那个工具用到的几个点」，不成链条。整个「插件是什么、插件之间
怎么依赖」一片空白，直接从第三级（配方合成）开始——因为那个工具恰好不需要问前两级。

**表述层**：实验说明里写着「复刻生产现场的风险用例」「验证某某走钢丝」。

**方法层**：最能说明问题的一句是「实验台要保真就得复刻那个工具的绕行」——**主从颠倒**。
实验台去复刻消费者的绕行，测出来的是绕行本身对不对，不是被绕的机制是什么。

物证：第一版的 PowerShell 版基线在 commit `c08eb8d`，六个实验的代码与结论均在其中。

## 依据

一个额外的观察坐实了这条转向：**最贴着下游视角的那个实验（E3），结果恰好是唯一
不可用的那个。**

因果不是巧合。E3 问的是「那个用法安不安全」而不是「这个机制怎么工作」，于是观测
方法也跟着从消费者的关注点出发——盯着那个工具会用到的探针，而不是盯着被改动的
那个条目。信号选错，整批数据作废（详见 0002 篇）。

**问题问错，观测就会跟着错**，这是选题原则能影响数据质量的具体路径。

## 代价与遗留

第一版的六个实验要用 Python 重写并复跑对齐。这个代价被接受，因为复跑同时提供了
另一层价值：**Python 版是独立的第二实现**，结论一致则是双实现交叉印证，
不一致则说明其中一版有 bug——两种情况都比只跑一遍强。
