# E3 观测信号选错导致整批数据作废

> 了断于 2026-08-15 ｜ 走死 ｜ 冻结

## 背景

第一版实验 E3 想回答：谁在监听 patch 文件、它什么时候会死。做法是反复改活层文件、
观察实例有没有反应。它报出了一个看起来很重要的「主发现」：活层重放只在 boot 后
第 1 次改动生效，第 2 次起彻底停摆，并称在两种拓扑下都复现、还排除了文件写入方式
的干扰。

## 结论与边界

**E3 整批数据不可用，三个假说全部「不可判」。** 原因是观测信号选错：
判定「活层重放是否发生」用的是**探针条目的 `appliedAt` 有没有变**，而那一轮改动
插入的是一个**跟探针无关的新条目**。

活层重放是**条目级 diff**——只挂载/卸载/重配变化的部分。探针条目自己没变，
它的 `appliedAt` 本来就不该变。正确的信号应该是「那个新插入的条目有没有出现」。

边界：**作废的是这批数据，不是那个问题**。「谁拥有 patch 监听、它什么时候死」仍然
是个真问题，只是要等观测方法论立住之后重做。E3 报的「哑火是永久性的」这个现象
本身**没有被证伪，也没有被证实**——它现在处于「没测过」状态，不是「已知不成立」。

## 经过

数据内部自相矛盾是最早的线索：同一份记录里 A2/C2 两轮又判定「生效」，
跟「第 2 次起彻底停摆」这个主发现直接打架。

顺着查观测信号，问题就明显了：整个 E3 用同一个探针条目的 `appliedAt` 当作
「重放发生了没有」的指示器，而各轮改动动的是别的条目。

物证：v1 的 E3 代码与原始读数在 commit `c08eb8d`。

## 依据

三条教训从这次作废里提炼出来，现在都固化在工作手册里：

1. **观测信号必须落在被改动的那个条目上。** 拿无关条目判断重放是否发生，
   得到的是「它本来就不该变」这个恒真结果
2. **dsh 的 web 应用对未匹配路径回 200 加 SPA 兜底 HTML，不回 404。** 探针判定
   必须校验响应体内容，只看状态码会假阳性
3. **活层里靠 `- insert:` 生出来的条目，后续每轮都要原样保留那个 insert 块。**
   换成裸 `- id:` 会让条目直接消失（那是覆盖语义，目标不存在时只警告并跳过）
   ——这跟「改配置没生效」是两种不同现象

还有一条更根本的，改变了整个课程的编排：**E3 想问的问题，前提是先把「配方热重放
本身怎么工作」搞清楚。** v1 把它当成已知背景直接跳过去，这是翻车的结构性原因——
**梯度不是排版顺序，是依赖顺序。**

## 代价与遗留

一整批实验数据报废。更贵的是它一度产出了一个像模像样的「主发现」，
如果没有内部矛盾露出马脚，它可能会被当成结论用下去。

遗留：重做时必须**单轮观测**——每轮改动前重启实例，一次只观测一件事，
避免多轮改动互相干扰。这条纪律已写进课程大纲对应那一课，未实施（那一课未开始）。
