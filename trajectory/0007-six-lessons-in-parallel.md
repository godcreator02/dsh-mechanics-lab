# 六课并行交给 subagent

> 了断于 2026-08-16 ｜ 采纳 ｜ 冻结

## 背景

基础十课（L0–L9）中 L0/L1/L2/L9 已完成，剩 L3–L8 六课。这六课要写教学插件、
写用例、跑通、写课程说明，逐课串行做工作量很大。

第一次尝试是只让 subagent 做「开课调研」（查文档、读源码、出用例清单），
代码与实验留给主会话。理由是这活的失败模式特殊：**用例写得看起来对、
但没在测它自称在测的东西**，而这种错误 subagent 会如实报告「全过」。

## 结论与边界

**六课可以完整交给 subagent 并行做**——写代码、自己跑、自己调，六课共 36 个用例
全部通过，加上原有的 34 个，全套 70 个用例互不干扰。

前提是先解决一个硬冲突并配一份反模式清单，两者缺一不可：

- 实验台的独占锁原本是**占用即抛错**，六个并行必炸。改成**排队等待**（最多 15 分钟，
  且只认活着的 pid，死进程的锁直接夺走）
- 每个 subagent 的交底里附**同一份反模式清单**，全是此前踩出来的坑

边界：**只验证了「调研 + 写代码 + 跑通」这一档**。用例质量的最终把关仍在主会话——
本次抽查了两课的关键用例，**没有逐课逐用例复核**。六课之间无强依赖（这是能并行的
前提），有强依赖的课能不能这么干，没测。

## 经过

先改锁（`experiments/lab/instance.py` 的 `acquire_lock`）：占用时轮询等待而非抛错，
并且只认活着的 pid——持锁进程崩了留下的锁文件会被直接夺走，不需要人工清理。
自测：手工写一个 pid 为 999999 的锁文件，新的 `acquire_lock` 成功夺走。

然后六个 subagent 并行，每个拿到：本课目标、已完成的调研结论（免得重查）、
现有课程的代码范例位置、**反模式清单**、产出要求、纪律。

反模式清单里的条目全部来自实际事故：

| 条目 | 出处 |
|---|---|
| `start_instance(wait_http=False)` 不做存活检查，判断启动失败必须自己看 `alive()` | 0003 篇 |
| 观测点绝不抛错，拿不到就如实记 `null` | L9 第一版探针 |
| 记录数组建在模块级、写文件写整个数组 | L0 普查员把自己被重挂藏住了 |
| dsh 对未匹配路径回 200 + SPA HTML，判定必须校验响应体 | v1 |
| 「验证什么都不该发生」用固定长等待，不能轮询提前退出 | v1 |
| 幽灵条目 id 随机，只能认 `name` | L0 |

结果：六课全部跑通（L3:8、L4:6、L5:9、L6:3、L7:6、L8:4 共 36 个用例），
全套 70 个用例 7 分 32 秒通过。公共脚手架未被越界修改（`git status` 核对）。

物证：六课的代码与课程说明在 `experiments/l03_*` 至 `experiments/l08_*`；
全套通过的记录见 `results/` 下对应时间戳的归档。

## 依据

能并行的关键不在「subagent 有多强」，而在**这批活的三个性质**：

1. **六课之间无强依赖** —— 各自扩展 L0 或复跑 v1 结论，不需要等对方的产出
2. **产物落在各自目录** —— 写代码天然不冲突，唯一的共享写入点是课程大纲（实测
   六个并发编辑没有互相覆盖，各改各的小节）
3. **跑实验的冲突可以排队解决** —— 冲突源是端口段与 profile 名，不是逻辑依赖，
   所以「串行执行」这个约束能用锁表达，不必牺牲并行写作

反模式清单是把「此前的事故」转成「下一个人的护栏」。它有效的证据是：
六课里没有一课重犯清单上的错误，而其中至少两条（存活检查、固定长等待）
正是主会话此前栽过的地方。

## 否掉的候选

- **串行派 agent，一次一个** —— 锁不冲突、agent 能自己迭代，但没有并行收益。
  否掉的直接原因是改锁的成本很低（一个函数），改完并行与安全可以兼得
- **subagent 只写代码、不许跑** —— 第一版方案。否掉的理由是没跑过的代码质量堪忧，
  主会话要逐课修语法、修 API 误用、修断言，工作量可能比自己写还大。
  写代码的人自己调试最快
- **不改锁，给每个 agent 分配独立 home 和端口** —— 会绕过实验台的安全机制
  （一次只跑一个实验是纪律的一部分），为并行牺牲隔离不划算

## 代价与遗留

抽查而非全查，意味着**可能有「假绿」用例混在 36 个里没被发现**。缓解措施是六课
产出的结论会在后续课程引用时被交叉检验（一条错的结论迟早跟别的结论打架——0003 篇
那次就是这么暴露的），但这是**事后的**兜底，不是当下的保证。

一处观察到但没深究的现象：三份开课简报互相补上了对方的盲区——L5 的调研诚实标了
「多层 patch 怎么串联，我没证实、不敢断言」，而 L4 和 L6 各自独立查清了。
**并行调研有交叉印证的副作用**，本次是白捡的，没有把它设计进流程。未实施。
