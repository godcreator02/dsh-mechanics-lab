# 全部运行产物的收口：假 home（out/testhome/）与观测归档（out/results/）。
# 一条规则管住，因为它们是同一种东西——**跑一次就能再生**。
# 进 git 的只有源：实验代码、教学插件、文档。观测的价值在于被消化进
# 各课 README 的结论，不在于原始 json 躺在仓库里。
/out/

# fixtures 里假插件被 pnpm 装出来的东西
**/node_modules/

# Python 虚拟环境：由 uv 管理，uv.lock 进 git 保证可复现，.venv 本身不进
/.venv/
__pycache__/
.pytest_cache/
