# 运行现场：假 home。它住 out/ 而不是各实验目录下，因为 profiles/*/node_modules/
# 是指向 npx 缓存与本仓源码的 junction 农场——进了源码树，任何递归遍历都会跟着
# 走出去（理由写在 lab/core.py 的 TESTHOME_ROOT）。
/out/

# 观测归档：跟着实验走，点开一项就看得到它跑出来什么。是纯文件、没有 junction，
# 所以可以住源码树里。跑一次就能再生，所以不进 git——观测的价值在于被消化进
# 各项 README 的结论，不在于原始 json 躺在仓库里。
experiments/**/results/

# fixtures 里假插件被 pnpm 装出来的东西
**/node_modules/

# Python 虚拟环境：由 uv 管理，uv.lock 进 git 保证可复现，.venv 本身不进
/.venv/
__pycache__/
.pytest_cache/
.ruff_cache/

# Claude Code 的本机权限决策与本地路径。settings.json 是共享正本、进 git，
# 这份不进
.claude/settings.local.json

# drafts phase 的临时载体：只在一轮方案打磨期间有意义，结论落 drafts/<topic>.md
.god-flow/drafts/NOTES.md
