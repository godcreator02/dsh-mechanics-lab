"""effect-vs-raw · 注册副作用时包不包 `ctx.effect`，卸载之后的差别

档次 ③ ｜ 性质 🔬 ｜ 状态 ⬜ ｜ 0 条用例 ｜ 需要 web

无用例。这里只写清要验什么、装置怎么搭，**不造用例**——验过之后再补进来。

## 要验什么

`ctx.effect(execute, label)` 会收集 `execute` 产生的清理函数，在 fiber 卸载时
按相反顺序执行；不经它、直接在 `apply` 里注册副作用（挂路由、开定时器、订阅
事件）则没有这层收集，条目卸载时框架不知道该收拾它。

`leak-on-reload` 已经验过定时器这条副作用线的下场（清理写漏的那版重载之后旧
定时器照样触发，两份并存）。本项要补的是**另一类副作用**：往 dsh 的 web 应用
上挂一个 HTTP 路由，比较「路由注册包不包 `ctx.effect`」，条目被禁用/卸载之后，
这条路由还能不能被访问到。

## 装置设想

两个条目各挂一个插件，都往 dsh-web-app 提供的 `webServer`（或等价的路由注册点）
上挂一条 GET 路由，返回一段带自身标记的响应体。差别只在挂路由那一行有没有包
`ctx.effect`：

- 写对的那版：`ctx.effect(() => { 挂路由(); return () => 卸路由(); })`
- 写漏的那版：直接 `挂路由()`，不登记清理动作

实例跑起来后先探一次两条路由都能访问、都返回各自的标记；然后把两个条目都
禁用掉（活层热重放），再探一次：写对的那版路由应当消失，写漏的那版路由应当
**还在**——这正是「卸载之后的差别」。

## 观测方法

⚠️ **探针判定的坑**：dsh 的 web 应用对未匹配路径回 **200 + SPA 兜底 HTML**，不回
404（`CLAUDE.md` 第五节记的坑，`recorder-reach` 一类的探针都踩过）。所以「路由还在
不在」不能只看状态码——写漏那版路由被判定为「消失」时，探针实际拿到的可能是
200 的兜底页而不是 404。探针必须校验**响应体内容**（比如自带的 marker 字段），
只看状态码会假阳性：把「路由确实还在」误判成「路由已经不在」，或者反过来把
兜底页误判成路由还活着。

沿用 HTTP 探测（`lab.instance` 里的 `http_get`），配合各路由自带的标记字段
校验响应体；`leak-on-reload` 已验的实例标记手法（每次挂载生成随机标记）可以
直接搬过来，用来分辨「路由还在」与「路由是新一轮挂上的同一条」。
"""
