"""effect-inventory · 直接读 fiber 上的 effect 清单

档次 ③ ｜ 性质 📗 ｜ 状态 ⬜ ｜ 0 条用例 ｜ 不需要 web

无用例。这里只写清要验什么、装置怎么搭，**不造用例**——验过之后再补进来。

## 要验什么

📗 官方文档 `docs/official/zh/cordis-api/fiber.md:197`：`fiber.getEffects()`
「返回当前已注册作用的元数据」，「每个带标签的活动作用对应一棵 `EffectMeta` 树」。

本项要验的是把这个 API 当**观测工具**用：给一个条目上的 `ctx.effect` 调用传
`label`，之后随时调 `fiber.getEffects()` 就能拿到一份「这个 fiber 此刻挂着
哪些活作用」的清单——不用等副作用自己暴露迹象（定时器触发、路由响应之类），
直接问 fiber 本身。

`fiber.getEffects()` 把「这个 effect 还在不在」从间接推断变成直接观测，而间接
观测正是「数据整批不可判」最典型的成因——`leak-on-reload` 与 `effect-vs-raw`
都在问同一类问题（一个副作用清理写没写对），现在的判定手法是等定时器触发、
探路由响应、看事件流里有没有出现幽灵标记，等不到动静时分不清是「真的清理
干净了」还是「还在、只是凑巧没触发」。`getEffects()` 把这件事变成直接观测：
卸载前后各调一次，比较清单里的 label 有没有变化，不用等它自己发声。

## 装置设想

一个条目注册若干个带 label 的 `ctx.effect`，apply 时立刻调一次
`ctx.fiber.getEffects()` 上报（起手快照）；再从外部触发卸载（禁用该条目），
在 dispose 回调里或卸载完成后再调一次（能调到的话——fiber 卸载后自身可能已经
拿不到，需要实测确认调用时机的边界）。比较两次快照里 label 集合的差集，跟
「哪些 effect 应该被清理」的预期对照。

## 观测方法

`getEffects()` 返回的是 `EffectMeta` 树，需要先实测它的具体形状（是否嵌套、
label 怎么取）再决定怎么摊平进事件流。这本身也是本项待验的一部分——不能假定
它跟 `loader.entries()` 那种扁平列表长得一样。
"""
