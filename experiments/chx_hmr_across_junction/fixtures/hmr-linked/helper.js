/**
 * 纯算法文件。它不导出插件、不碰 ctx、不注册任何东西 —— 只是被 index.js
 * `import` 进去的一个函数。
 *
 * ⑫⑭ 改的就是这里，一个字都不碰 index.js：
 *
 *   ⑫ hmr 开着 —— 看重来的是这个文件，还是整个插件入口
 *   ⑭ hmr 关着 —— 看插件在改动之后接着调用它，能不能拿到新实现
 */

export function compute() {
  return "算法第一版";
}
