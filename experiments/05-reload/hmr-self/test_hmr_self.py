"""hmr-self · hmr 自己也住在自己触发的重放射程里

档次 ③ ｜ 性质 🔬 ｜ 状态 ⬜ ｜ 0 条用例 ｜ 不需要 web

## 现状

**没有用例。** hmr 是一个普通条目，跟其他任何条目一样住在树上、能被同一套
条目级配方重放机制改动。问题是：改 hmr 自己的 `config` 会不会把它自己的
patch 监听搞断、而且断了不恢复；它的更新钩子能不能否决自己被重启这件事。

## 要验什么

1. 改 hmr 条目的 `config`（比如换 `root`、`ignored`、`debounce`）触发的是
   配方热重放，这次重放会不会重建 hmr 自己的 watcher；重建期间原有的
   watcher 有没有一段被自断又没恢复的窗口
2. 重放 hmr 自己这件事，跟重放别的普通条目相比有没有特殊路径（比如源码里
   是否对「被重放的条目恰好是 hmr 自己」做了特判）

## 实验设计要点（没有用例但设计已想清楚，供开工时直接抄）

- **必须单轮观测。** 一次 pytest 用例只能验一次「改 hmr 的 config 之后
  它还灵不灵」——改完之后无论灵不灵，hmr 自身的状态都已经被这次重放改变
  过，不能在同一个实例里接着做第二轮同类观测，得出的结论会混进「上一轮
  重放留下的影响」
- **每轮改动前必须重启实例**，不能在同一个实例里连续测多种 hmr 自己的
  config 改法——理由同上，每一轮都要从一个干净的、行为已知的起点开始
- **必须有「改别的条目」的对照组。** 只测「改 hmr 自己」测不出「重放这件事
  本身有没有连带影响」——如果改任何条目（不只是 hmr）都会导致后续热重载
  变得不可靠，那问题出在重放机制本身，不是 hmr 特有的自指问题。对照组是
  同一套装置改一个跟 hmr 无关的普通条目，两边一比才能把「重放本身的代价」
  和「hmr 自己被重放」这两件事分开

## 没覆盖到的

整项待建。
"""
