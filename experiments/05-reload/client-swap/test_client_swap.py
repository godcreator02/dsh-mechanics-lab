"""client-swap · 浏览器端产物变了之后的热换链路

档次 ③ ｜ 性质 🔬 ｜ 状态 ⬜ ｜ 0 条用例 ｜ 需要 web

## 现状

**没有用例。** `docs/GLOSSARY.md` 记着一条尚未落实验的观察：client 侧（浏览器
端产物）用的是**轮询**而不是 inotify 类文件系统事件去感知变化（源码注释写着
「网络挂载不发 inotify 事件」），生效延迟约 3 秒，跟 host 侧 hmr 的约 1-2 秒
是两条完全独立的链路，不共用同一个 watcher。

## 要验什么

链路是：轮询 → 发现 client 产物变了 → 重算摘要（内容 hash） → 换版本号 →
推给浏览器。要验的点：

1. 轮询周期与生效延迟的量级（对照 GLOSSARY 记的约 3 秒）
2. 摘要怎么算的、什么粒度（单文件还是整个产物目录）
3. 版本号变化如何推给浏览器——是浏览器主动轮询接口，还是服务端有推送
4. 这条链路与 host 侧代码热重载（hmr 项）是否共享任何监听基础设施，还是
   完全独立两套

## 没覆盖到的

整项待建：需要叠 `dsh-web-app` bundle 并起浏览器端观测面，装置比本组其余
各项更重。
"""
