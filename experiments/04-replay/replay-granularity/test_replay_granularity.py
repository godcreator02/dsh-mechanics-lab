"""replay-granularity · 条目级 diff 到底 diff 在哪一层

档次 ② ｜ 性质 🔬 ｜ 状态 ⬜ ｜ 0 条用例 ｜ 需要 web 待定

## 要验什么

`replay-mechanism`（同组）坐实了配方热重放的入口：`entry.update({config})`
改的是 `cordis:include` 这一个条目的 config，重新读文件、重新 compose 出
`patches` 数组，塞进去。这一项要往下追一层：**那份新的 `patches` 数组交给
`include` 子树之后，哪些条目会被重挂、哪些不会——判定的粒度落在哪一层？**

已知但尚未坐实的推论有两条，来自本项目其它已验证条目对 `include` 子树的旁观：

- **只有内容真的变了的那个条目会重挂。** `cold-surfaces`（同组）的三条用例
  已经从外部观测到：改一个跟被测条目无关的文件，被测条目的 `appliedAt` /
  `moduleLoadedAt` 纹丝不动——这是「条目级 diff 存在」的间接证据，但那几条
  用例的设计目的是验证「某个面是冷的」，不是正面验证 diff 算法本身。
- **但推测粒度要往下挪一层：每次重放时，`include` 子树下所有条目的 `options`
  都被无差别地重新赋值一遍**（走 `loader/partial-dispose` 一类的内部事件），
  只是没有变化的那些条目的 **fiber** 没有被动过（不重新创建、不重新 apply）。
  也就是说「只有变了的条目会重挂」这句话里的「重挂」，指的是 fiber 层面的
  重新创建，不是 config 对象层面的替换——**config 对象本身，包括没变的那些，
  可能每次都被换了一份新的**。

这条区分很要紧：如果观测信号选在「这个条目的 `config` 对象引用变没变」，
测出来的会是「全体都变了」，跟「只有变的条目重挂」这条已知结论表面冲突——
但两者其实是同一件事的两个不同层面，选错观测信号会把这个非冲突误判成
「推翻了已知结论」。

## 为什么先立观测方法论、不动手造用例

CLAUDE.md 观测方法论第 1 条就是本项目的教训：**条目级 diff 意味着「只有变化
的条目会重挂」，所以判断「活层重放是否发生」，观测信号必须落在被改动的那个
条目上；拿一个无关条目的 `appliedAt` 判断重放有没有发生是错的——它本来就
不该变，得到的是恒真结果。** 这条纪律本身就是从早期实验的一次误判里提炼出来
的，但它只回答了「该拿哪个条目当信号」，没有回答「信号该落在 fiber 层还是
config 层」——本项要补的正是这一层。

设计一条能把「fiber 没动」与「config 对象被换了」分开的用例，需要能直接读到
两种不同粒度的观测点（比如 fiber 的身份〔要么是 fiber 自身的对象引用，要么
是 `fiber.getEffects()` 之类的清单〕，和 config 对象的引用或某种指纹），
现有教学插件都没有暴露这两层区分。造这样的用例之前，先要确认：

- 哪个内部事件/钩子能可靠地区分「fiber 被重新创建」与「config 被替换但 fiber
  还是原来那个」——这需要先读源码（`cordis-plugin-loader` 的 `config/entry.ts`
  与 `config/tree.ts`），`08-service-core` 组 `effect-inventory` 项已经在用
  `fiber.getEffects()` 做类似的事，可以借鉴同一套观测手法
- 对照组怎么搭：至少要有「改了的条目」「没改的条目」「同一子树里但完全不相关的
  条目」三种，才能把「只有变化的条目重挂」这条结论的边界钉清楚

「验证什么都不该发生」用足够长的固定等待（10 秒起），不能轮询提前退出——提前
退出只能证明「此刻还没发生」，证明不了「不会发生」；「验证应该发生」用轮询
重试，更稳也更快。

这些都还没做，所以本项没有用例、没有 fixtures，只把观测方法论和设计要点写清楚，
留给真正开工时用。
"""
