"""pending-timing · 等不到的依赖，boot 期与运行期的两种待法

档次 ③ ｜ 性质 ⚠️ 矫正型 ｜ 状态 ⬜ 未覆盖 ｜ 0 条用例 ｜ 不需要 web

## 要验什么

同一件事——一个条目的 `inject` 永远等不到服务——在两个时机下的处置：

- **boot 期：无条件致命。** 这一半已经由 `boot-audit` 立住并有实验支撑：
  `dsh-app-boot` 的 `assertEntriesActivated` 在 `boot()` 返回前审计每一个
  未 disabled 的条目，PENDING 一律抛错、整棵树回滚、进程退出码 1。
  详见 `../boot-audit/test_boot_audit.py` 的模块 docstring，本项不重复
  验证这一半。
- **运行期：安静地待着，不会致命。** 这是本项缺的那一半——boot 结束后
  靠热重放新加一个永远等不到服务的条目，预期它停在 PENDING、进程继续
  活着、没有任何审计会把它揪出来杀掉进程。这一半目前只有源码依据
  （`assertEntriesActivated` 只在 `boot()` 内被调用一次，不在热重放路径
  里），**没有实验验证过**。

## 为什么还没验

实验设计依赖 `04-replay`（配方热重放的手段：往活层追加一个新条目、观察
它上树）与 `duplicate-id-timing` 已经验过的「运行期变更被兜住」同一类
观测方法——需要先有一个能在运行中安全追加条目的稳定装置。取材表没有
列出对应的旧用例（`chx_hmr_across_junction` 那批用例改的是既有条目，
不是从零插入一个新的、永远等不到服务的条目），所以本项目前判定
**未覆盖**，不代造用例。

## 没覆盖到的

- 运行期新增的 PENDING 条目会不会被之后某一次热重放意外带活（比如它
  依赖的服务后来上线了）——这是「补上那一半」之后自然该验的下一步，
  但目前连基础那一半都还没有用例。
- `FAILED` 态（`apply` 抛异常）与 `PENDING` 态在审计里是两条不同分支
  （`FAILED` 会 `await fiber.await()` 取回原始 rejection）——这条只在
  旧版大纲里留了一句话，没有对应实验，也不在本项的取材范围内，点名
  记在这里以免遗漏。
"""
