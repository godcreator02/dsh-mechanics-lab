"""externals-exit · 入口依赖树一变，整个进程退出

档次 ③ ｜ 性质 🔬 发现型 ｜ 状态 ⬜ 未覆盖 ｜ 0 条用例 ｜ 不需要 web

## 要验什么

hmr 主 watcher 收到文件变化时按四条分支处置，其中一条是：文件在
`externals`（CLI 入口自身的依赖树，不是插件的依赖树）里 → `loader.exit()`
——**整个进程退出**。这是 hmr 唯一一条主动重启进程的路：其余三条分支
分别是刷新某个 include 子树（配方热重放）、代码热重载（ESM loadCache
命中）、或者只发一个没人接的 `hmr/change` 事件。

跟本组其它项的关系：这是本组唯一一条**由代码变化触发、结果是主动退出
进程**的路径，跟 `boot-audit`/`duplicate-id-timing` 里「审计判死刑」或
「配置更新失败」导致的退出是完全不同的触发源——那些是「等不到东西」或
「撞车」判定出的死刑，这条是 hmr watcher 自己认出「入口本身变了」之后
主动选择重启。放进本组是因为它同样是「进程会不会退出」这条轴上的一个
判例，即使触发机制跟本组其余各项不同源。

## 为什么还没验

目前只有源码依据（hmr 主 watcher 四条分支判断逻辑），没有实验验证过
`externals` 具体指哪些文件、改动它们是否真的触发 `loader.exit()`、以及
退出时机与退出码。取材表没有列出对应的旧用例，**未覆盖**，不代造用例。

## 没覆盖到的

- `externals` 集合的准确定义（CLI 入口自身的依赖树具体包含哪些文件，
  跟插件依赖树的边界在哪）——待实测时需要先从源码把这个集合的算法读
  清楚，再设计能安全触发它的改动。
- 退出时机（是否等当前 in-flight 的操作完成）与退出码，都没有实验数据。
"""
