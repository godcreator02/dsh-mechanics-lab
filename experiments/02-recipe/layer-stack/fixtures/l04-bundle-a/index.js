/**
 * 教学组合包的插件模块本体。
 *
 * 本项绝大多数用例只用 `--dump-config` 做静态分析，从不真正启动实例——
 * 四层怎么叠是配方层面的事，用不着拉一个真进程去看。写这个空操作 apply
 * 只是保险：万一某个用例真的拉起了实例，条目能正常激活，不会卡在
 * 「找不到模块」上。
 */
export const name = "l04-bundle-a";

export function apply() {
  // 静态用例，故意什么都不做。
}
