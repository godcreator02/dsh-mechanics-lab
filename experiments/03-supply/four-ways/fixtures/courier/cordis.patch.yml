# 包自己带的那一行——「这个包想挂哪个条目」写在包里，跟着包一起交付。
#
# 途径③（正经安装）靠的就是它：装完之后 profile 的 patch 文件里一个字都不用写，
# 条目照样出现。途径②④不用它——那两条路要在 patch 文件里自己写 insert。
- insert:
    - id: courier
      name: ch2-courier
      config:
        途径: 正经安装
