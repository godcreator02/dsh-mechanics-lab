"""isolation-guarantees · 实验台自己的隔离靠机制，不靠纪律

档次 ② ｜ 状态 ⬜ 未覆盖 ｜ 0 条用例 ｜ 不需要 web

## 要验什么

实验台并行跑各组用例时依赖四条隔离机制，目前全都只有**设计与源码依据**，
没有专门验证它们本身成立的用例：

- **假 home 按项分。** `conftest.py` 的 `lab_home` fixture 用测试文件所在目录名
  当标签（`Path(str(request.fspath)).parent.name`），据此建 `out/testhome/<标签>/`。
  没验证：两个目录名不同的项，是否真的从不共用同一个 home——目前只是「目录名
  全局唯一」这条纪律加上文件系统天然保证唯一性，没有用例去故意制造重名场景
  并断言它被拒绝或至少不会互相污染。
- **端口按 worker 静态分段。** `conftest._my_ports()` 把 `LAB_PORT_RANGE`
  （3090–3099）按 xdist worker 数切段，每个 worker 只在自己那一小段里扫。
  没验证：并发场景下两个 worker 是否真的不会拿到同一个端口——现状是「检查空闲
  到真正起进程之间有个窗口」这类竞态在设计评审里被排除了，但没有专门构造高并发
  场景去实测这条边界。
- **会话锁只挡两个会话，不挡 session 内部各 worker。** `pytest_configure`/
  `pytest_unconfigure` 用 `acquire_lock("lab")`/`release_lock()`，且只有主进程
  （非 xdist worker）才拿锁。没验证：两个真实会话同时起跑时，后来者是否真的会
  排队等待而不是互相踩踏 patch 文件——这条在 CLAUDE.md 里明确写了历史事故
  （v1 时两个会话并发跑互相覆盖对方的 patch 文件，靠人眼看出来的），但事故本身
  没有留下可复跑的回归用例。
- **产物归档不误伤失败现场。** `_archive()` 在 `lab_home` fixture 的 `finally`
  块里执行，成败都归档。没验证：归档失败时是否真的不会掩盖用例本身的失败结果
  （代码里有 `except Exception` 兜底并打印警告，但没有用例故意让归档失败去验证
  这条兜底真的不会让原本失败的用例变绿）。

## 为什么还没验

这四条都是**实验台自己的基础设施**，不是 DSH 框架本身的机制——按项目
`CLAUDE.md` 第一节「唯一的研究对象是 DSH 框架本身」，它们本来就不属于教材/
判定要覆盖的范围，只是作为「后面每一项站在什么上面」的地基，理应被列进
00-base 的覆盖清单里如实标记未覆盖，而不是被悄悄略过。

验证这四条需要**故意制造隔离失效的场景**（比如真的开两个并行 worker 抢同一个
端口段边界、真的用两个会话打同一个 home），设计和运行成本明显高于其他 00-base
项，且验证过程本身有撞坏其他正在跑的实验的风险（纪律边界见 CLAUDE.md 第四节）。
搬迁阶段没有这类用例可取材，也不在本次任务范围内新造——按取材表要求，
**没有配套实验的知识不进大纲**，本项如实标 ⬜ 未覆盖。
"""
