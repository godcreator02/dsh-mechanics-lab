# CLAUDE.md — dsh-mechanics-lab

DeepSeek Harness（DSH）插件系统原理的**独立研究项目**。两个产物：一套可复跑的实验，
和一份从简单到困难讲透原理的教材。

## 一、核心原则（先读这条，它决定一切选题）

> **唯一的研究对象是 DSH 框架本身。**

⚠️ **这个项目与任何下游工具无关，特别是与 dshw 无关。** 实验台不服务于任何具体
工具、不为谁的用法背书、不验证谁的做法对不对。带着某个下游工具的疑问过来的话，
那些疑问归它自己的项目——**这里不接**。

具体到操作上：

- **实验选题从「原理链条」出发**。问的是「插件系统怎么工作」，不是「某某这么用对不对」
- **实验装置不仿制任何真实插件**。fixtures 是为讲清原理而设计的教学插件
- **教材只讲框架原理**，不出现任何具体下游工具的名字

**为什么这条排在最前**：以某个消费者为参照系组织选题，会让实验只覆盖「那个消费者
用到的几个点」而非完整原理链条；更要命的是**观测方法会跟着从消费者的关注点出发**
——问的是「这个用法安不安全」而不是「这个机制怎么工作」，选出来的观测信号就会
偏，跑出来的数据整批不可判。**问题问错，观测就会跟着错。**

## 二、教学梯度（实验与教材共用的骨架）

**顺着加载链条走**，六部分十七课：

> L0 全景 → **插件与条目** → **配方** → **树** → **激活** → **效果与清理** → **热**
>
> 你写的东西 → 声明它的那一行 → 那一行从哪来 → 变成什么 → 怎么活过来 → 活着的怎么变

L0 是「先总后分」的总：一次看清整棵树的形状，后面六部分各展开一块。

**完整大纲在 `docs/SYLLABUS.md`**（每课的性质、要回答什么、实验设计、难点、依赖与开销），
这里不重复，改动只改那一处。

⚠️ **梯度是依赖顺序，不是排版顺序。** 后一课要用前一课立住的词和观测手段。
跳级的具体后果是**观测信号选不对**：不先搞清「重放发生时什么该变、什么不该变」，
就设计不出能判定「hmr 归属」的信号，跑出来的数据整批不可判。

### 每课都要标性质，它决定用例怎么写

| 性质 | 含义 | 用例写法 |
|---|---|---|
| 📗 复述型 | 官方文档已写明 | **对照式**：断言文档承诺的行为；一致则复述，不一致是重大发现 |
| 🔬 发现型 | 文档没有，只能实测 | **观察式**：先如实记录发生了什么，再下判定 |
| ⚠️ 矫正型 | 文档写了但简化或不全 | **对比式**：文档说 X，实测 X 的边界在哪 |

**开课第一件事是查 `docs/official/`
判定本课属于哪一类**——探索式用例写成「跑跑看会怎样」，对照式写成「文档承诺 X，
验证是不是 X」，断言强度和失败时的含义完全不同。

## 三、工作方式

- **每级先定观测方法，再写实验。** 观测信号选错会产出看似有效、实则不可判的数据
  ——而且它往往能报出一个像模像样的「发现」，不靠内部矛盾露马脚就发现不了。
- **写作可以并行，跑实验必须串行。** 无强依赖的课可以同时写（产物落在各自目录，
  天然不冲突）；跑实验的冲突源是端口段与 profile 名，靠锁排队解决，不必手工协调。
  有强依赖的课照旧按依赖顺序来。
- 教材 `index.html` 跟着梯度长，不预先铺满占位。

## 四、实验纪律（违反会打到生产实例上）

| 项 | 规矩 |
|---|---|
| **home** | 只用假 home `.testhome/`（`DSH_HOME` 经子进程 `env` 传入，不改当前进程环境）。**绝不碰 `~/.dsh`** |
| **端口** | 只用 3090–3099。**绝不碰 3080（主实例）/ 3239 / 3733** |
| **删目录** | 逐层拆 junction，**绝不跟着链接走**（profile 的 node_modules 里全是指向源码和 npx 缓存的 junction） |
| **并发** | 一次只跑一个实验——多个实验共用 profile 名和端口会互相污染。锁会排队，不必手工协调 |
| **Python 环境** | **只用 uv + venv，禁止全局 Python。** 解释器钉死 3.12.10（`.python-version`），依赖锁在 `uv.lock`，一切经 `uv run` 执行 |
| **语言** | Python。进程编排、YAML 解析、断言都在 `experiments/lab/` 包里 |

## 五、观测方法论（都是踩出来的）

1. **条目级 diff 意味着「只有变化的条目会重挂」。** 所以判断「活层重放是否发生」，观测信号
   必须落在**被改动的那个条目**上。拿一个无关条目的 `appliedAt` 判断重放有没有发生是错的
   ——它本来就不该变，得到的是恒真结果。
2. **dsh 的 web 应用对未匹配路径回 200 + SPA 兜底 HTML，不回 404。** 探针判定必须校验
   响应体内容（比如自带的 `marker` 字段），只看状态码会假阳性。
3. **活层里靠 `- insert:` 生出来的条目，后续每轮都要原样保留那个 insert 块。** 换成裸
   `- id:` 覆盖会让条目直接消失（那是「覆盖已有条目」语义，目标不存在时只警告并跳过）
   ——这跟「改配置没生效」是两种不同现象，别混。
4. **「验证什么都不该发生」用足够长的固定等待**（10 秒起），不能用轮询提前退出；
   **「验证应该发生」用轮询重试**（500ms 一次），更稳也更快。
5. **YAML 输出要真解析，不要正则抓。** 正则块提取现在能跑，但 dump 格式一变就会**静默失真**
   ——测试工具最不该有的性质就是悄悄变绿。

## 六、结论的地位

### 信息源按「便宜且权威」排序取用

**官方文档 → 源码 → 实测。三档都要走，一档都不能跳。**

⚠️ **每开一课，第一件事是 grep `docs/official/`。**

那是官方 `docs/` 的完整快照，215 篇，中英各一棵树（`zh/` 105 篇是平时看的，
`en/` 110 篇——多出的 5 篇是仓库内部工作文件，没有中文版）。两侧目录结构一致，
同一条相对路径就是同一篇的两个语言版本。中文不是机翻，仓库有双语配对门禁。

**版本钉死在 commit `47f9438`，不跟 `master`。** 理由是我们的结论都是对着某一版
文档说「这条写了 / 没写 / 写得不一样」的——拉了新版，所有这类对照就失去基准，
分不清是我们看错了还是文档后来改了。要更新就换个目录另存一份，两版并列。

重拉：用 GitHub API 取 `master` 的 SHA，按 SHA 拉 `docs/**.md`，
再按 `*.zh.md` → `zh/`（去后缀）、其余 → `en/` 重组。

⚠️ 那个目录里**不放我们自己写的东西**——它是纯净的官方快照。
官方对这套文档自己的说明在 `en/AGENTS.md`。

跳过第一档的代价是**重复劳动**：官方文档已写明的机制会被当成待发现的东西重测一遍。

反过来也不能只信文档，两条边界：

- 文档在教程语境里会做**简化**——比如「插件路径必须是绝对路径」，那是建议不是解析
  规则的全部（Windows 上真正的规则是「必须是合法 URL」，裸盘符路径会报
  `ERR_UNSUPPORTED_ESM_URL_SCHEME`）
- 文档只讲**「怎么写插件」**，不讲**「框架怎么加载插件」**——幽灵条目、boot 末尾
  审计、树的层级这些全在后一层，官方没有对应篇目

**三档互相校验**才是对的做法。文档说的和实测不符时，**那种不一致恰恰是最有价值的
产出**——但要分清是「文档做了简化」还是「文档与实现真的偏离」。

### 🔑 读源码先看有没有 `src/`——别读打包产物

**9 个包在 npm 包里带了 TypeScript 原文件**，而且正好是最关键的那几个：

| 包 | `src/` 内容 |
|---|---|
| `cordis` | 9 个 .ts，97 KB —— Fiber / Context / Service 的正身 |
| `cordis-plugin-loader` | 7 个 .ts —— `config/{entry,group,tree,utils,isolate}.ts` |
| `cordis-plugin-include` | `src/index.ts` —— **patch 三种语义的唯一实现** |
| `cordis-plugin-hmr` | 2 个 .ts |
| `cordis-plugin-timer` / `cordis-plugin-group` / `cosmokit` / `schemastery` | 小 |

路径：`<npx 缓存>\node_modules\@deepseek-ai\<包名>\src\`。

前几课一直在读 `lib/index.js`（打包后、744 行、变量名压过），**白费了很多力气**——
TS 原文件带完整 JSDoc 和类型标注、按职责分文件，同样的问题往往一眼可见。

⚠️ **例外**：`dsh-*` 系列（`dsh-app-boot`、`dsh` 本体）**没有 `src/`**，
只能读 `lib/` 打包产物。所以「先 `Test-Path <包>\src`」应该是读源码的第一个动作。

### 实测 > 源码推理

源码推理最典型的两种错法：

- **只读函数体、不追调用点。** 一段逻辑清晰、注释详尽的代码可能根本没被调用——
  可选参数尤其危险，它的**默认分支**往往才是实际走的那条。**从源码得出机制解释，
  必须追到调用点确认参数真的传了**
- **把源码位置当判定依据。** 「这里写着抛错所以不会缓存」这类推断要实测才算数

所以：
- 每条写进教材的判定都要标注状态——**待验 / 已实测 / 已推翻**
- 引用文档要标出处与版本（`docs/official/zh/user/develop/basic/publish.md:123`），
  不要写「官方说……」——文档会改，标了版本才知道对照的是哪一版
- 源码位置可以写进「为什么」，但不能单独作为判定依据

## 七、结论落在哪：现状与探索历史分两套

| | 现状文档 | 探索历史 |
|---|---|---|
| 装什么 | 项目此刻是什么样 | 探索史：实验、放弃的路线、推翻的判断 |
| 形态 | **覆盖式更新**，过期就改 | **一次了断后冻结**，写完不改 |
| 判据 | 这句话半年后还代表当前真相吗 | 这承载的是探索意志吗 |
| 在哪 | `CLAUDE.md`、`docs/` 下平铺的几篇、各课 README | `docs/trajectory/` |

**现状文档不装历史叙事**——推翻、演进、事故史一概不留。判定看时态词在修饰谁：
修饰的是本篇所述对象自己、说的是它从什么变成了什么，就是违反。

处置只问一件事：**把时态剥掉之后，损失的东西里有没有「为什么现在必须这样」。**

- **有，就剥掉时态、把判据留在原地。** 判据是当前设计的约束，读者要靠它才敢动实现。
  反例：「这里第一版栽过跟头，用例靠 `except LabError` 判断启动失败，结果……」；
  正例：「`start_instance(wait_http=False)` 立即返回、不做存活检查，判断启动失败
  必须自己看 `inst.alive()`」——同一份信息，时态没了，判据全在
- **没有，就整段删。** 搬家改名这类事实 git 里有；试过哪些路、怎么翻的车归 `docs/trajectory/`

现状文档还**不装**：时间戳与相对时间、changelog 段落、对 `trajectory/` 篇目的引用。
`trajectory/` 则**不装**现状与待办——篇是冻结的，装现状等于生产假消息。

同一件事在两处各有一句话**不算两份真相**：「兜底 hmr 与 include 平级」是现状，
「这条怎么发现的、推翻了什么」是历史，两个不同的事实。

⚠️ **删掉的探索经过不必都转去 `docs/trajectory/`。** 够格记的探索在了断发生的当下就该记，
那时证据还在手边；拖到整理文档才发现它躺在正文里，说明当时判定它不够格，
事后补记也补不出当时的证据。整理文档只管文档自己整洁，不兼职判断哪段历史该留档。

### 一个事实一个家

每个事实只在**它归属的那一处**写，别处提名不复述。判定手法很直接：
**拿一个有特征的短语去 grep，命中多处就说明有重复的家**——留一处，其余改成提名。

⚠️ 引用密集往往不是链接的毛病，是**边界切错了**：两篇互相复述对方结论、
改一篇必得看另一篇才敢动，那是同一个变化轴被硬拆成两份，合并；
一篇装多个独立主题、被不同地方各取一段，拆开。
**先审边界，再谈引用**——加链接和复制粘贴都是边界病的止痛药。

### 写文档前过一遍这张清单

这几条来自 DSH 官方仓库自己的文档规范（`docs/official/en/AGENTS.md`），
是人家在自己仓库里天天跑的门禁，直接适用：

- **叙事化历史** —— `previously` / `now` / `no longer` / `used to` / `renamed` /
  `was moved` / 「以前」「后来」「改成了」。改动故事归 commit 与 `docs/trajectory/`
- **实现状态标注** —— `implemented!`、`future: …`、「已完成」「待实现」。
  状态会烂，仓库布局和 manifest 自己就承载它
- **手抄的目录与清单** —— 源码或生成器才是权威的东西（包清单、事件表、
  测试列表），手抄一份必然漂移
- **推理过程流水账** —— 逐步实现叙述、显然分支的论证、测试走查、
  被否掉的局部方案。**留下结论契约与耐久的理由，删掉推导它的那条路**
- **理由重复在兄弟条目旁边** —— 该写在它归属的那个能力上，写一次
- **段落墙** —— 一段扛好几条规则加一堆插入语。拆开，或把细节降到它的家
- **强调通胀** —— 到处 bold、到处「关键」「务必」，就等于没有重点。
  **强调留给真正改变行为的那半句**

注释与 docstring 同理：**写完整的契约，不写推理记录**。要保留的是行为、失败、
时序、所有权、例外、后果、以及不显然的取向；要删的是叙述、走查、代码复述。

**直白地写**：点名具体的检查、类型、API、操作、行为，不用「网关」「词汇表」
「表面」这类隐喻。

### 篇幅超了先搬、再压、最后才放宽

文档写长了，处置顺序是固定的：**① 把属于别处的内容搬走**（原处留一句提名），
**② 压缩确实属于这里但可以更短的**，**③ 才考虑它本来就需要这么长**。

⚠️ 官方那套还有分层（architecture / subsystems / Agent Notes / postmortem /
cookbook）与字数门禁，那是为**那个仓库**设计的，跟本项目的两套分工不是一回事，
**别照搬**。

## 八、目录

```
CLAUDE.md          本文件——怎么在这个项目里干活
GLOSSARY.md        术语表：按 Cordis / Loader / DSH / 自造 四层分组，标注哪些已实测
SYLLABUS.md        课程大纲：六部分十七课，每课的性质／实验设计／依赖与开销
README.md          箱子说明：破例声明、端口占用、怎么跑
trajectory/        ⚠️ 探索历史（冻结）：转向、走死、推翻的判断
                   跟现状文档的分工与写法见其 README.md
docs/official/     ⚠️ 官方 docs 的纯净快照（版本钉 47f9438）。**开课前先 grep 这里**
  zh/              中文 105 篇  ← 平时看这个
  en/              英文 110 篇；`en/AGENTS.md` 是官方对这套文档自己的说明
                   两侧目录结构一致，同一路径即同一篇的两个语言版本
                   最值钱的三处：cordis-api/（从源码注释生成，带行号）、
                   subsystems/（每个子系统一篇）、postmortem/（官方事故报告）
demo/              教具：lab-inspector（DSH 内嵌面板，兼 L16 的 client 插件样板）
observatory/       观测台：lab-recorder（注入式采集）+ 看板（独立只读服务）
index.html         教材（结构早于当前梯度，含已被推翻的判定，待处置）
pyproject.toml     依赖与 pytest 配置
.python-version    3.12.10，uv 据此建 venv
uv.lock            依赖锁定，进 git 保证可复现
experiments/
  conftest.py      pytest 装置：假 home、端口、实例回收、**产物归档**
  lab/             公共脚手架（包）
    core.py        路径常量、junction 安全删除、LabHome / LabProfile、基线 profile
    dump.py        --dump-config 真解析（含 !!js 方言）
    instance.py    实例起停、并发锁、HTTP 探测
  l00_minimal_environment/   一课一目录，各自完全自包含
    README.md      这一课讲什么、实测到了什么
    test_l00.py    pytest 用例
    fixtures/      本课专用教学插件（允许跨课重复，那是特性）
  l01_… l02_… l09_…（编号即教学顺序，见 docs/SYLLABUS.md）
results/           每次跑的归档（summary.md + 见证文件进 git，*.jsonl 不进）
.testhome/         假 home，gitignore
.venv/             虚拟环境，gitignore
```

跑法一律经 uv，不激活也不碰全局解释器：

```powershell
uv sync                              # 首次或依赖变更后
uv run pytest -m static              # 只跑不起进程的实验
uv run pytest experiments/l01_minimal_plugin/   # 单跑一课
```
