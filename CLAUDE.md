# CLAUDE.md — dsh-mechanics-lab

DeepSeek Harness（DSH）插件系统原理的**独立研究项目**。两个产物：一套可复跑的实验，
和一份从简单到困难讲透原理的教材。

## 一、核心原则（先读这条，它决定一切选题）

> **唯一的研究对象是 DSH 框架本身。**

⚠️ **这个项目与任何下游工具无关，特别是与 dshw 无关。** 实验台不服务于任何具体
工具、不为谁的用法背书、不验证谁的做法对不对。带着某个下游工具的疑问过来的话，
那些疑问归它自己的项目——**这里不接**。

具体到操作上：

- **实验选题从「原理链条」出发**。问的是「插件系统怎么工作」，不是「某某这么用对不对」
- **实验装置不仿制任何真实插件**。fixtures 是为讲清原理而设计的教学插件
- **教材只讲框架原理**，不出现任何具体下游工具的名字

**为什么这条排在最前**：以某个消费者为参照系组织选题，会让实验只覆盖「那个消费者
用到的几个点」而非完整原理链条；更要命的是**观测方法会跟着从消费者的关注点出发**
——问的是「这个用法安不安全」而不是「这个机制怎么工作」，选出来的观测信号就会
偏，跑出来的数据整批不可判。**问题问错，观测就会跟着错。**

## 二、实验怎么组织

两条**正交**的轴，各管各的：

- **组**回答「这个机制归谁管」——一个机制一个家，不因为排序被撕开
- **档**回答「什么时候做它」——**标在项上，不标在组上**

档标在项上是有意的。标在组上就必然出现一个叫「边界情况」的组，而边界本该住在
它所属的那个机制旁边。于是 `05-reload` 里 `watch-root` 是①档、`hmr-self` 是③档，
两条都还住在它们该住的地方。

| 档 | 含义 | 谁会撞上 |
|---|---|---|
| ① | 天天碰 | 写插件、装插件、改完要生效 |
| ② | 要懂才用得好 | 想让结果可预测、想知道改一处波及多远 |
| ③ | 深水区与边界 | 造服务的人；出了事回头查的人 |

**完整清单在 `docs/SYLLABUS.md`**——十组、每项的档次与覆盖状态。那是索引，
**判定住每一项自己的 `test_*.py` 模块 docstring**，改结论只改那一处。

⚠️ **一档往前排是有代价的**：三档的 `boot-audit` / `boot-failure-shapes` 排在最后，
但一档的实验先用得上它们——判「这次跑挂了」要认得出启动失败长什么样。所以一档实验里
按已知结论用，09 组开工时再坐实回填；**09 组一旦推翻某条前提，前面引用过它的项要复核**。

### 每项都要标性质，它决定用例怎么写

| 性质 | 含义 | 用例写法 |
|---|---|---|
| 📗 复述型 | 官方文档已写明 | **对照式**：断言文档承诺的行为；一致则复述，不一致是重大发现 |
| 🔬 发现型 | 文档没有，只能实测 | **观察式**：先如实记录发生了什么，再下判定 |
| ⚠️ 矫正型 | 文档写了但简化或不全 | **对比式**：文档说 X，实测 X 的边界在哪 |

**开工第一件事是查 `docs/official/`
判定本项属于哪一类**——探索式用例写成「跑跑看会怎样」，对照式写成「文档承诺 X，
验证是不是 X」，断言强度和失败时的含义完全不同。

## 三、工作方式

- **先定观测方法，再写实验。** 观测信号选错会产出看似有效、实则不可判的数据
  ——而且它往往能报出一个像模像样的「发现」，不靠内部矛盾露马脚就发现不了。
- **一项一目录、一份用例、一份 fixtures，判定写在模块 docstring 里。**
  没有 README——判定跟着代码走，读用例的人不用翻第二个文件。
- **各项可以并行做**：产物落在各自目录，天然不冲突。跑实验默认
  `-n 10 --dist loadfile`，用端口的项打 `xdist_group` 钉住 worker。
  单项调试或要看清输出时加 `-n 0` 关掉并行。
- **⬜ 未覆盖的项照样建目录**，里面放一个光有 docstring、没有用例函数的
  `test_*.py`，写清要验什么、为什么还没验、实验设计要点。开工时直接在同一个
  文件里加用例。**绝不为了凑数造用例。**

## 四、实验纪律（违反会打到生产实例上）

| 项 | 规矩 |
|---|---|
| **home** | 只用假 home `out/testhome/`（`DSH_HOME` 经子进程 `env` 传入，不改当前进程环境）。**绝不碰 `~/.dsh`** |
| **端口** | 只用 3090–3099。**绝不碰 3080（主实例）/ 3239 / 3733** |
| **删目录** | 逐层拆 junction，**绝不跟着链接走**（profile 的 node_modules 里全是指向源码和 npx 缓存的 junction） |
| **`dsh plugin`** | 只许对假 home 里的 profile 跑、只许装本地目录。**绝不对生产 home 跑、绝不装会联网拉取的包**——危险的是这两件事，不是这条命令本身 |
| **并发** | 各课并行跑，隔离靠 home 分课 + 端口按 worker 静态分段（`conftest._my_ports`）。锁只防两个**会话**同时开跑，会排队，不必手工协调 |
| **Python 环境** | **只用 uv + venv，禁止全局 Python。** 解释器钉死 3.12.10（`.python-version`），依赖锁在 `uv.lock`，一切经 `uv run` 执行 |
| **语言** | Python。进程编排、YAML 解析、断言都在 `experiments/lab/` 包里 |

## 五、观测方法论（都是踩出来的）

1. **条目级 diff 意味着「只有变化的条目会重挂」。** 所以判断「活层重放是否发生」，观测信号
   必须落在**被改动的那个条目**上。拿一个无关条目的 `appliedAt` 判断重放有没有发生是错的
   ——它本来就不该变，得到的是恒真结果。
2. **dsh 的 web 应用对未匹配路径回 200 + SPA 兜底 HTML，不回 404。** 探针判定必须校验
   响应体内容（比如自带的 `marker` 字段），只看状态码会假阳性。
3. **活层里靠 `- insert:` 生出来的条目，后续每轮都要原样保留那个 insert 块。** 换成裸
   `- id:` 覆盖会让条目直接消失（那是「覆盖已有条目」语义，目标不存在时只警告并跳过）
   ——这跟「改配置没生效」是两种不同现象，别混。
4. **「验证什么都不该发生」用足够长的固定等待**（10 秒起），不能用轮询提前退出；
   **「验证应该发生」用轮询重试**（500ms 一次），更稳也更快。
5. **YAML 输出要真解析，不要正则抓。** 正则块提取现在能跑，但 dump 格式一变就会**静默失真**
   ——测试工具最不该有的性质就是悄悄变绿。
6. **跨两程的实验，先确认读到的是这一程的数据。** 观测产物按固定路径写，新实例起来才会
   把它清空重写。重启之后立刻轮询「它说话了吗」，会**当场读到上一程的残留**并判定成功
   ——重启前把上一程的产物挪开（顺带归档），或者等文件真被重写了再读。
   这条跟第 4 条是一对：轮询本身没错，错在轮询的对象可能是旧的。

## 六、结论的地位

### 信息源按「便宜且权威」排序取用

**官方文档 → 源码 → 实测。三档都要走，一档都不能跳。**

⚠️ **每开一课，第一件事是 grep `docs/official/`。**

那是官方 `docs/` 的完整快照，215 篇，中英各一棵树（`zh/` 105 篇是平时看的，
`en/` 110 篇——多出的 5 篇是仓库内部工作文件，没有中文版）。两侧目录结构一致，
同一条相对路径就是同一篇的两个语言版本。中文不是机翻，仓库有双语配对门禁。

**版本钉死在 commit `47f9438`，不跟 `master`。** 理由是我们的结论都是对着某一版
文档说「这条写了 / 没写 / 写得不一样」的——拉了新版，所有这类对照就失去基准，
分不清是我们看错了还是文档后来改了。要更新就换个目录另存一份，两版并列。

重拉：用 GitHub API 取 `master` 的 SHA，按 SHA 拉 `docs/**.md`，
再按 `*.zh.md` → `zh/`（去后缀）、其余 → `en/` 重组。

⚠️ 那个目录里**不放我们自己写的东西**——它是纯净的官方快照。
官方对这套文档自己的说明在 `en/AGENTS.md`。

跳过第一档的代价是**重复劳动**：官方文档已写明的机制会被当成待发现的东西重测一遍。

反过来也不能只信文档，两条边界：

- 文档在教程语境里会做**简化**——比如「插件路径必须是绝对路径」，那是建议不是解析
  规则的全部（Windows 上真正的规则是「必须是合法 URL」，裸盘符路径会报
  `ERR_UNSUPPORTED_ESM_URL_SCHEME`）
- 文档只讲**「怎么写插件」**，不讲**「框架怎么加载插件」**——幽灵条目、boot 末尾
  审计、树的层级这些全在后一层，官方没有对应篇目

**三档互相校验**才是对的做法。文档说的和实测不符时，**那种不一致恰恰是最有价值的
产出**——但要分清是「文档做了简化」还是「文档与实现真的偏离」。

### 🔑 读源码先看有没有 `src/`——别读打包产物

**9 个包在 npm 包里带了 TypeScript 原文件**，而且正好是最关键的那几个：

| 包 | `src/` 内容 |
|---|---|
| `cordis` | 9 个 .ts，97 KB —— Fiber / Context / Service 的正身 |
| `cordis-plugin-loader` | 7 个 .ts —— `config/{entry,group,tree,utils,isolate}.ts` |
| `cordis-plugin-include` | `src/index.ts` —— **patch 三种语义的唯一实现** |
| `cordis-plugin-hmr` | 2 个 .ts |
| `cordis-plugin-timer` / `cordis-plugin-group` / `cosmokit` / `schemastery` | 小 |

路径：`<npx 缓存>\node_modules\@deepseek-ai\<包名>\src\`。

前几课一直在读 `lib/index.js`（打包后、744 行、变量名压过），**白费了很多力气**——
TS 原文件带完整 JSDoc 和类型标注、按职责分文件，同样的问题往往一眼可见。

⚠️ **例外**：`dsh-*` 系列（`dsh-app-boot`、`dsh` 本体）**没有 `src/`**，
只能读 `lib/` 打包产物。所以「先 `Test-Path <包>\src`」应该是读源码的第一个动作。

### 实测 > 源码推理

源码推理最典型的两种错法：

- **只读函数体、不追调用点。** 一段逻辑清晰、注释详尽的代码可能根本没被调用——
  可选参数尤其危险，它的**默认分支**往往才是实际走的那条。**从源码得出机制解释，
  必须追到调用点确认参数真的传了**
- **把源码位置当判定依据。** 「这里写着抛错所以不会缓存」这类推断要实测才算数

所以：
- 每条写进教材的判定都要标注状态——**待验 / 已实测 / 已推翻**
- 引用文档要标出处与版本（`docs/official/zh/user/develop/basic/publish.md:123`），
  不要写「官方说……」——文档会改，标了版本才知道对照的是哪一版
- 源码位置可以写进「为什么」，但不能单独作为判定依据

### 没有配套实验的知识不进 SYLLABUS

判定进大纲的前提是**有能复跑的用例支撑**。只有观察、还没实验的，进
`.god-flow/drafts/`——那里是待实现池，等对应那一课开工时把它设计成用例；
验过了，结论才进 `docs/SYLLABUS.md` 和课 README，draft 里对应那段随之清掉。

⚠️ 这条不是洁癖。SYLLABUS 是判定的清单，往里塞未验证的观察，等于**用大纲的
权威给没验过的东西背书**——读的人无从分辨哪条有实验、哪条只是印象。
`.god-flow/drafts/` 走 god-flow 的 draft 规则：每版独立完整重写、不写演进史、
不写「待定 / TBD」（真分叉出复选框题，决了就写成结论）。

## 七、结论落在哪：现状与探索历史分两套

> 本节是 god-flow `time-tense` 哲学（见文末）在本项目的落地形态——判据与反例
> 都按实验台的场景写死了，动手看这里，问「为什么要这样分」看那条哲学。

| | 现状文档 | 探索历史 |
|---|---|---|
| 装什么 | 项目此刻是什么样 | 探索史：实验、放弃的路线、推翻的判断 |
| 形态 | **覆盖式更新**，过期就改 | **一次了断后冻结**，写完不改 |
| 判据 | 这句话半年后还代表当前真相吗 | 这承载的是探索意志吗 |
| 在哪 | `CLAUDE.md`、`docs/` 下平铺的几篇、各课 README | `docs/trajectory/` |

**现状文档不装历史叙事**——推翻、演进、事故史一概不留。判定看时态词在修饰谁：
修饰的是本篇所述对象自己、说的是它从什么变成了什么，就是违反。

处置只问一件事：**把时态剥掉之后，损失的东西里有没有「为什么现在必须这样」。**

- **有，就剥掉时态、把判据留在原地。** 判据是当前设计的约束，读者要靠它才敢动实现。
  反例：「这里第一版栽过跟头，用例靠 `except LabError` 判断启动失败，结果……」；
  正例：「`start_instance(wait_http=False)` 立即返回、不做存活检查，判断启动失败
  必须自己看 `inst.alive()`」——同一份信息，时态没了，判据全在
- **没有，就整段删。** 搬家改名这类事实 git 里有；试过哪些路、怎么翻的车归 `docs/trajectory/`

现状文档还**不装**：时间戳与相对时间、changelog 段落、对 `trajectory/` 篇目的引用。
`trajectory/` 则**不装**现状与待办——篇是冻结的，装现状等于生产假消息。

同一件事在两处各有一句话**不算两份真相**：「patch 里写的条目全住在 include 的
子树里」是现状，「这条怎么发现的、推翻了什么」是历史，两个不同的事实。

⚠️ **删掉的探索经过不必都转去 `docs/trajectory/`。** 够格记的探索在了断发生的当下就该记，
那时证据还在手边；拖到整理文档才发现它躺在正文里，说明当时判定它不够格，
事后补记也补不出当时的证据。整理文档只管文档自己整洁，不兼职判断哪段历史该留档。

### 一个事实一个家

> god-flow `ssot` 哲学（见文末）在本项目的落地形态。

每个事实只在**它归属的那一处**写，别处提名不复述。判定手法很直接：
**拿一个有特征的短语去 grep，命中多处就说明有重复的家**——留一处，其余改成提名。

⚠️ 引用密集往往不是链接的毛病，是**边界切错了**：两篇互相复述对方结论、
改一篇必得看另一篇才敢动，那是同一个变化轴被硬拆成两份，合并；
一篇装多个独立主题、被不同地方各取一段，拆开。
**先审边界，再谈引用**——加链接和复制粘贴都是边界病的止痛药。

### 写文档前过一遍这张清单

这几条来自 DSH 官方仓库自己的文档规范（`docs/official/en/AGENTS.md`），
是人家在自己仓库里天天跑的门禁，直接适用：

- **叙事化历史** —— `previously` / `now` / `no longer` / `used to` / `renamed` /
  `was moved` / 「以前」「后来」「改成了」。改动故事归 commit 与 `docs/trajectory/`
- **实现状态标注** —— `implemented!`、`future: …`、「已完成」「待实现」。
  状态会烂，仓库布局和 manifest 自己就承载它
- **手抄的目录与清单** —— 源码或生成器才是权威的东西（包清单、事件表、
  测试列表），手抄一份必然漂移
- **推理过程流水账** —— 逐步实现叙述、显然分支的论证、测试走查、
  被否掉的局部方案。**留下结论契约与耐久的理由，删掉推导它的那条路**
- **理由重复在兄弟条目旁边** —— 该写在它归属的那个能力上，写一次
- **段落墙** —— 一段扛好几条规则加一堆插入语。拆开，或把细节降到它的家
- **强调通胀** —— 到处 bold、到处「关键」「务必」，就等于没有重点。
  **强调留给真正改变行为的那半句**

注释与 docstring 同理：**写完整的契约，不写推理记录**。要保留的是行为、失败、
时序、所有权、例外、后果、以及不显然的取向；要删的是叙述、走查、代码复述。

**直白地写**：点名具体的检查、类型、API、操作、行为，不用「网关」「词汇表」
「表面」这类隐喻。

### 篇幅超了先搬、再压、最后才放宽

文档写长了，处置顺序是固定的：**① 把属于别处的内容搬走**（原处留一句提名），
**② 压缩确实属于这里但可以更短的**，**③ 才考虑它本来就需要这么长**。

⚠️ 官方那套还有分层（architecture / subsystems / Agent Notes / postmortem /
cookbook）与字数门禁，那是为**那个仓库**设计的，跟本项目的两套分工不是一回事，
**别照搬**。

## 八、目录

```
CLAUDE.md          本文件——怎么在这个项目里干活
README.md          箱子说明：破例声明、端口占用、怎么跑
god-settings.json  god 系列项目级配置：root-id（留空，本项目不接 vault）+ hook 开关
.claude/
  settings.json    plansDirectory 指向 .god-flow/plans/
.god-flow/         god-flow 机制的工作目录——当前真相文档归 docs/，这里只装过程态
  drafts/          待实现池：还没有用例覆盖的观察与方案，走 god-flow 的 draft 规则。
                   验过之后结论进那一项的 docstring，这里对应那段清掉
                   NOTES.md 是一轮打磨期间的临时载体，不进 git
  plans/           计划落盘处（Plan Mode 自动落这）
  reports/         留档报告（审查／盘点／调研）：写完冻结，进 git
docs/
  GLOSSARY.md      术语表：按 Cordis / Loader / DSH / 自造 四层分组，标注哪些已实测
  SYLLABUS.md      覆盖清单：十组每项的档次与状态。**判定不在这里，在各项 docstring**
  trajectory/      ⚠️ 探索历史（冻结）：转向、走死、推翻的判断
                   跟现状文档的分工与写法见其 README.md
  official/        ⚠️ 官方 docs 的纯净快照（版本钉 47f9438）。**开工前先 grep 这里**
    zh/            中文 105 篇  ← 平时看这个
    en/            英文 110 篇；`en/AGENTS.md` 是官方对这套文档自己的说明
                   两侧目录结构一致，同一路径即同一篇的两个语言版本
                   最值钱的三处：cordis-api/（从源码注释生成，带行号）、
                   subsystems/（每个子系统一篇）、postmortem/（官方事故报告）
observatory/       观测台：lab-recorder（注入式采集，provide labObserver）
                   + board/（独立只读看板）
pyproject.toml     依赖与 pytest 配置
.python-version    3.12.10，uv 据此建 venv
uv.lock            依赖锁定，进 git 保证可复现
experiments/
  conftest.py      pytest 装置：假 home、端口、实例回收、**产物归档**
  lab/             公共脚手架（包）
    core.py        路径常量、junction 安全删除、LabHome / LabProfile、基线 profile
    dump.py        --dump-config 真解析（含 !!js 方言）、load_yaml
    events.py      读事件流
    instance.py    实例起停、并发锁、HTTP 探测
  00-base/                          组 = 机制的归属面，编号只提供稳定排序
    minimal-profile/                项 = 一个能单独跑的实验
      test_minimal_profile.py       用例 + **模块 docstring：判定的家**
      fixtures/                     本项专用教学插件（允许跨项重复，那是特性）
      results/                      归档，gitignore，留最近三次
    …
  01-entry/ 02-recipe/ 03-supply/ 04-replay/ 05-reload/
  06-inject/ 07-tree/ 08-service-core/ 09-boot-vs-runtime/
out/               运行现场，整个 gitignore（跑一次就能再生）
  testhome/        假 home，一项一个子目录；跑那一项时先清空再重建。
                   ⚠️ 它住这儿不住实验目录下——理由见 lab/core.py 的 TESTHOME_ROOT
.venv/             虚拟环境，gitignore
```

⚠️ **⬜ 未覆盖的项也有目录**，里面是一个光有 docstring、没有用例函数的 `test_*.py`。
所以「目录数」不等于「有用例的项数」，`--collect-only` 收不到它们。

跑法一律经 uv，不激活也不碰全局解释器：

```powershell
uv sync                                          # 首次或依赖变更后
uv run pytest -m static                          # 只跑不起进程的实验
uv run pytest experiments/01-entry/              # 单跑一组
uv run pytest experiments/01-entry/disabled/     # 单跑一项
```

---

## god-flow 简介

本项目走 god-flow 流程开发：新会话先接续 `.god-flow/plans/` 里的未完计划，方向或方案还没定先打磨再动手，方案定了才出计划，照计划分步落地、随手提交、随手按载体判据落文档——这是本项目的开发主循环，完整方法论见 workflow skill。

## 动手前的调用约束

| 域                             | 动手前的调用约束                                                 |
| ------------------------------ | ------------------------------------------------------------------ |
| 写代码 / 审代码 / 配工具链 / 写测试 | 先调 code skill 取对应场景的规范，不许凭印象写、凭感觉审           |
| 需求方案 / 出计划 / 发版        | 先调 workflow skill 对应动作，不许跳过直接开工或手拍版本号         |
| 项目文档 / 报告 / 规则          | 先调 writing skill 对应动作，不许自定格式                          |
| 记探索历史                      | 按第七节写进 `docs/trajectory/`，不许自定格式（本项目特例，见文末）  |
| 文字规范整形审查                | 先调 audit skill，不许凭感觉改措辞                                  |
| 骨架接入与全量对齐              | 先调 setup skill，不许手工拼骨架                                    |
| 写 Python / TS                 | 先在 code skill 里认语言、取对应语言包，不许跳过语言包直接下笔      |

## 常驻底线

以下几条没有「调 skill」的触发时机——要么绑在会话启动 / 提交时刻 / 写任何文件这种无处不在的时刻上，要么绑在 AI 自己识别的事件上，等不到用户开口说触发词，只能常驻在此：

- 新会话动手前先查 `.god-flow/plans/` 有没有未完成计划，有就先 Read 接续，不依赖对话记忆。
- commit 自动做：一件事做完就提交，不要问「要不要 commit」、也不要等用户发话。
- 一逻辑一提交，不要攒到最后一次性提交；一轮活儿收尾时自己的改动全部提交干净、不留尾巴。
- 多会话并行是常态，提交一律用 `git commit --only <path>` 锁自己的路径；新建的文件 `--only` 认不了，先 `git add <path>` 显式列具体路径再 `--only` 提交；不小心把别人的改动也提了不回退，接受现状继续即可。
- 项目的 md 文档一律住 `docs/`，不散落在项目根或其他目录（CLAUDE.md / README.md 例外）。
- 临时产物（调试 dump、一次性脚本、中间结果）一律写 `out/`。
- 文件名一律用英文。
- 探索出现改变下一步动作的了断（采纳 / 转向 / 走死 / 被取代 / 新方向起步 / probe 结论）时，在回复末尾一句话提醒「记一笔」，绝不代写；用户开口要求才落，落点是 `docs/trajectory/`。

## 交互协议

- 方向类决策（选型 / 划边界 / 定价值观，没有客观标准、纯靠主观偏好）：落锤 100% 归用户，AI 不许自己定了就落盘。
- 技术判定类（对照客观标准的判定，如回归 PASS/FAIL、semver bump、违规清单）：AI 出结论 + 证据，不躲「我不能替您判断」。
- 琐碎细节（commit、读哪份文件、内部工具调用选择）：AI 自动做掉，不停下来问；用户临时喊停的场次例外。
- **要向用户提问之前先调 ask skill 取问法，不许凭习惯直接问**——什么该问什么不该问、怎么把选项递过去、同类怎么合并，判据与格式全在那里。

## 设计哲学

god-flow 体系 6 条全域哲学——指导「为什么这样做」、不规定「具体怎么做」，任何场景全程伴随。每条 4 字段：一句话主张 / 模糊场景的自问 / 反例 / 正例。编程哲学 4 条不在此列——只在写代码时相关，住 code skill 规范集的 code-philosophy 篇、由代码场景按需加载。

### ssot

- 一句话：每条信息只在一处定义；其他位置需要这条信息时**引用**它的位置、不复述内容
- 自问：「这个信息别处已经定义了吗？」「改 A 时还要改 B 吗？」「这是『主源』还是『冗余备份』？」
- 反例：同一概念多处各写说明（改了 A 忘改 B）/「冗余备份」为了「读者方便」多处复述同一规则 / 链接断了不补（迫读者绕回原文档去搜）/ 双 SSOT 残留（Artifact 已实现、设计层文字描述还在并存）
- 正例：单一定义 + 链接引用 / SSOT 形态演化——实现前真相在 `drafts/`、实现后真相在 Artifact 本身、drafts/ 对应段删除

### time-tense

- 一句话：文档按「信息时态」严格二分——现状文档永远反映当前真相、覆盖式更新、不留历史叙事；追加文档永远只记历史、写入后不修改 / 不删除 / 编号不复用
- 自问：「这条信息半年后还代表当前真相吗？」是的话进现状文档，不是、只是某时刻发生过就进追加文档
- 反例：现状文档夹带历史叙事（反模式清单见 writing 组时态一节）/ 追加文档改旧条 / 编号复用占已删槽位
- 正例：现状文档推翻即覆盖、历史进探索历史档案 / tag annotation 等追加载体、只追加不回改

### think-before-act

- 一句话：复杂改动（跨多文件 / 跨多场景 / 不确定方案 / 不可逆）先 plan、再动手。plan 是过程——列影响范围 / 改动顺序 / 风险点、用户拍板后再动手；不是写文档
- 自问：「这是『复杂改动』吗？」跨多文件重构 / 重命名 / 删整目录 / 新引入抽象 / 调目录结构 / 改哲学或写作正本，都算复杂改动，先 plan
- 反例：用户喊「重构 X」立即开 Edit（没想清楚边界）/ 边做边想做到一半发现走偏推倒重来 / 跳 plan 直接对话拍方案（上下文没 anchor 容易飘）
- 正例：列影响范围 + 改动顺序 + 风险点，用户拍板后动手

### ai-role-bifurcation

- 一句话：决策分两类、AI 边界不同——方向类（做什么 / 不做 / 价值观 / 主观偏好）AI 出完整方案 / 候选对比 + 推荐、落锤 100% 归用户；技术判定类（对照客观标准的判定）AI 出结论 + 证据、用户终审
- 自问：「这个决策有客观标准吗？」有的算技术判定类（AI 出结论 + 证据），没有、是偏好 / 价值观的算方向类（AI 给方案和推荐、用户拍板落锤）
- 反例：方向类越权——用户没拍板 AI 就把推荐当结论落盘 / 只列利弊不表态让用户自己猜 / 技术判定逃避——「我不能替您判断 X 是否 PASS」
- 正例：方向类出完整方案或候选对比 + 优缺点 + 推荐、用户标注 / 拍板后才落锤（如选技术栈 / 两候选选哪个 / 边界划在哪）/ 技术判定 AI 出结论、不躲（如回归测试判 PASS/FAIL / semver 推荐 / audit 违规清单）

### human-at-the-edges

- 一句话：能让 AI 自主连跑的流程、人只在入口（定方向 / 给授权）和出口（验结果 / 背书）两端介入、中段不逐步打断；卡点设在哪由「错了出口能不能拦住 + 拦不住的代价多大」决定、不由「看着不放心」决定
- 自问：「这个确认点是真有不可逆后果非拦不可、还是只是习惯每步看一眼？」错了出口能兜、产物可覆盖的收进出口、中段放手；错了不可逆且出口兜不住的留同步卡点
- 反例：自动流程每步都停下等确认、把「信任 AI 自主」退化成「AI 提线木偶」；或反过来、不可逆动作（改飞已审 evidence）也不设人闸门
- 正例：入口一次性授权放手跑、中段产物可覆盖（错了重跑覆盖旧的）、收尾异步背书两端把关、中段零打断；不可逆动作仍设用户授权闸门

### progressive-disclosure

- 一句话：按需加载上下文、不一次性 dump。主索引永远先加载、详细内容**按需** Read 触发。「看一眼全貌」是例外、不是日常
- 自问：「这个 Read 是『现在就要』还是『可能用到』？」可能用到的不 Read，现在要的才 Read
- 反例：`@file: phases/*.md` 全量 include（假装分文件、实际等同单文件）/ 一次性 Read 多份 phase 文件（除非用户真要全览）/ 规则文件之间互相 include（一个规则带出 5 个无关规则）
- 正例：主 skill 索引 + 各 phase / reference 按需 Read 触发 / 「看全貌」走显式触发、不默认全装

## 本项目对 god-flow 的两处特例

改了默认规矩就得在这里记账，否则下次跟插件对齐时分不清哪些差异是有意的：

- **轨迹落 `docs/trajectory/`，不落 Obsidian vault。** 本项目不接 vault，没有项目根卡，
  `god-settings.json` 的 `root-id` 留空。上面调用约束表里「记探索历史」那行因此指向第七节，
  不走 vault-ops skill——它是落卡机械，本项目没有卡可落。
- **提交门禁只跑前三步，`uv run pytest` 不进门禁。**

  ```powershell
  uv run ruff format .
  uv run ruff check --fix .
  uv run pyright
  ```

  判据是 `experiments/` 下的 pytest 不是回归测试，是**实验装置**：它真起 dsh 实例、
  占 3090–3099、全套 75 秒，跑出来的观测数据本身就是产物之一。把它绑进「一件事做完就
  提交」的底线，既拖垮提交节奏，又会让多会话并行提交时撞端口。实验照第三节手动跑。
